# Rust engine workspace

This is the Rust port of Forge's Java rules engine. Read the root `/AGENTS.md` first, plus `docs/agents/PARITY_PHILOSOPHY.md`. Most parity work bottoms out here.

## Required reading: the DSL spec

Card scripts are the source of truth for behavior; the engine evaluates them. Before any work that touches abilities, triggers, replacements, static abilities, costs, the stack, SVars, selectors, or layered effects, read:

- **`docs/forge-dsl-semantics.md`** — runtime model: execution pipeline, stack semantics, targeting, cost payment, layer system, SVar resolution, determinism invariants, known CR deviations.
- **`docs/forge-dsl-grammar.md`** — syntax / parsing model. Read this when working on the parser, the IR, or interpreting an unfamiliar param.

When you write or change engine logic, write IRs and resolution code that mirror the semantics in the spec exactly. If your implementation diverges from the spec, either the implementation is wrong or the spec needs updating — don't silently drift.

## Crate map

All under `manabrew-rs/crates/`. The relay, hub, and self-hosted node each have a topic doc under `docs/agents/` — read it before touching that crate.

| Crate                      | Role                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Java mirror                                  |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| `forge-foundation`         | Pure types: colors, mana, card types, zones, phases. No I/O.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | `forge/forge-core/`                          |
| `forge-card-script`        | Card script IR shared between parser and engine.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                                            |
| `forge-cardset-archive`    | rkyv archive format for the pre-compiled cardset, plus the `build-cardset-archive` and `emit-forge-assets` binaries and the framed-bundle builder the Web Image engine embeds. Consumed by `forge-carddb` for ~6× faster cold start in packaged builds.                                                                                                                                                                                                                                                                                                                                 | —                                            |
| `forge-carddb`             | Parses Forge's 32K+ `.txt` card scripts into `CardRules`, with an rkyv fast-path via `forge-cardset-archive`. WASM-safe (no `std::fs`).                                                                                                                                                                                                                                                                                                                                                                                                                                                 | `forge/forge-game/.../CardRules.java` etc.   |
| `manabrew-engine-macros`   | Build-time proc macros. No runtime logic.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —                                            |
| **`manabrew-engine`**      | The rules engine. Game state, turn loop, combat, stack, abilities, replacements, triggers, statics, costs, mana.                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | `forge/forge-game/src/main/java/forge/game/` |
| `manabrew-agent-interface` | DTO layer between the engine and any agent (UI, AI, network). Defines the prompt protocol.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —                                            |
| `manabrew-protocol`        | Engine-free DTOs shared by the clients: decks, game-domain types (`ManaColor`, `GameFormat`, `EngineKind`), prompts, display events, agent transport envelopes. Published. Holds no relay message and never depends on the relay crate.                                                                                                                                                                                                                                                                                                                                                 | —                                            |
| `manabrew-relay-protocol`  | The relay wire protocol: `ClientMessage`/`ServerMessage`, the session handshake (`Authenticate`, `AuthResult`, `SessionTakenOver`), lobby management (`RoomInfo`, `PlayerInfo`, `DraftConfig`, `SealedConfig`), and `PROTOCOL_VERSION`. Depends on `manabrew-protocol` for `Deck` and the domain enums; nothing there depends back. **`PROTOCOL_VERSION` is this crate's major**, so a breaking wire change ships as a major release here, never as a separate constant. Owns the `gen-protocol` bin, which sits at the top of the protocol graph and emits TypeScript for both crates. | —                                            |
| `manabrew-game-runtime`    | Hosts an N-player game session over a caller-supplied transport. `host_runtime::run_hosted_multiplayer_game` is the single source of truth for the multiplayer setup-and-run dance (Tauri uses mpsc, WASM uses SharedArrayBuffer — both supply their transport via closures).                                                                                                                                                                                                                                                                                                           | —                                            |
| `networking-tests`         | System-wide networking regression suite: spawns the real relay + node binaries and drives real websocket clients (production `manabot` for bot seats). One Given/When/Then per test; `yarn test:networking` locally, build-checks CI serialized. Custom `libtest-mimic` harness (`harness = false`): each test is a plain `async fn` that must be registered in `main()` — a bare `#[tokio::test]`/`#[test]` will compile but never run.                                                                                                                                                | —                                            |
| `loadgen`                  | Load generator: drives real games against a relay so whatever hosts them can be measured under load. Claims hosted rooms by default; with `ROOM` it joins a named room instead, which covers a browser hosting a table. Real protocol, `manabot` decisions, bot seats. Env-configured; see its README.                                                                                                                                                                                                                                                                                  | —                                            |
| `parity`                   | Differential test harness — runs Rust and Java side-by-side and compares. See its own AGENTS.md.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                                            |
| `manabrew-gym`             | Reinforcement-learning environment: one game thread per env with a channel-backed learner agent, `VecEnv`, and the fixed-size observation encoder. See its own AGENTS.md.                                                                                                                                                                                                                                                                                                                                                                                                               | —                                            |
| `manabrew-server`          | The relay: standalone matchmaking/lobby server. Optional. See `docs/agents/RELAY.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | —                                            |
| `manabrew-hub`             | Deck Hub + stats + auth API behind `api.manabrew.app`. See `docs/agents/HUB.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | —                                            |
| `wasm`                     | wasm-bindgen exports for browser builds.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —                                            |
| `manabot`                  | The bot that fills empty seats: rules over the player view (`agent/simple_ai.rs`), no engine access. `card_roles.tsv` (removal, sweeper, counterspell, ramp, draw, tokens, ...) is generated, not hand-edited: phase-ai's structural detectors run over phase's card-data export; regenerate it when the card pool changes. In-process seats (`PromptAgent<BotResponder>`) get their view through `Responder::present`; relay seats get it from the state envelope (`state.rs`).                                                                                                        | —                                            |
| `self-hosted-node`         | Headless engine node (AI bots, automation). See `docs/agents/SELF_HOSTED_NODE.md`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —                                            |

Dependency direction: `foundation`, `card-script`, `cardset-archive` ← `carddb` ← `engine` ← everything else, and `manabrew-protocol` ← `manabrew-relay-protocol`. Don't introduce cycles.

## `manabrew-engine` crate — module map

The engine itself lives at `manabrew-rs/crates/manabrew-engine/src/`. Each module mirrors a Java package under `forge/forge-game/src/main/java/forge/game/`.

| Module                                    | What it owns                                                                                                                                                                                                                                                       |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `game.rs`, `core.rs`, `action.rs`         | Central `Game` state, state-based actions, top-level mutations.                                                                                                                                                                                                    |
| `game_loop/`                              | Turn driver, priority loop, stack resolution, phase handler, action_space (legal-action enumeration). The hot path.                                                                                                                                                |
| `phase/`                                  | Phase types, extra phases, extra turns, untap step.                                                                                                                                                                                                                |
| `ability/`                                | `SpellAbility` factory, IR, dispatch. `api_type.rs` lists every supported `SP$/AB$/DB$` API.                                                                                                                                                                       |
| `ability/effects/`                        | The 200+ `*_effect.rs` resolvers. **Most parity bugs land here.** Has its own AGENTS.md.                                                                                                                                                                           |
| `spellability/`                           | `SpellAbility` family — targeting, restrictions, conditions, optional costs.                                                                                                                                                                                       |
| `staticability/`                          | Continuous/static effects with the CR 613 layer system.                                                                                                                                                                                                            |
| `replacement/`                            | Replacement effects (37 types). Each `replace_*.rs` provides `can_replace` + `execute` + a callsite into the loop.                                                                                                                                                 |
| `trigger/`                                | Triggered abilities (~120 `trigger_*.rs`). Each maps to a Forge `Mode$`.                                                                                                                                                                                           |
| `cost/`                                   | Cost framework (`CostX` family), payment, decision-maker, visitor.                                                                                                                                                                                                 |
| `combat/`                                 | Attack/block declaration, constraints, damage assignment.                                                                                                                                                                                                          |
| `mana/`                                   | Mana pool, payment, conversion matrix, auto-pay.                                                                                                                                                                                                                   |
| `card/`                                   | `CardInstance`, collections, perpetual effects (`perpetual/`), tokens (`token/`), card factory.                                                                                                                                                                    |
| `player/`                                 | `PlayerState`, life, statistics, predicates, controller, plus `actions/` (typed player actions).                                                                                                                                                                   |
| `zone/`                                   | `MagicStack`, cost-payment stack. (Zone _types_ live in `forge-foundation`.)                                                                                                                                                                                       |
| `keyword/`                                | Static keywords (Flying, Trample, Suspend, Kicker, Modular, …). One file per keyword.                                                                                                                                                                              |
| `agent/`                                  | `PlayerAgent` trait and helpers. The engine never decides — it asks the agent.                                                                                                                                                                                     |
| `parsing/`                                | Engine-side parsers (amount expressions, comparators, key/value). Distinct from `forge-carddb`.                                                                                                                                                                    |
| `svar/`                                   | Forge SVar evaluator (variables embedded in scripts).                                                                                                                                                                                                              |
| `mulligan/`                               | Opening hand / London mulligan.                                                                                                                                                                                                                                    |
| `lki.rs`, `game_snapshot.rs`, `game_log*` | Bookkeeping: last-known-info, snapshots, log entries.                                                                                                                                                                                                              |
| `census.rs`                               | Opt-in telemetry, off by default: which script parameters were read per API, and which permissive fallbacks fired. Call `census::unhandled(kind, detail)` from any new arm that answers an unknown script string with a default. Read with `parity census-report`. |

## Symptom → folder

Use this when a parity report points at a specific failure mode:

| Symptom                                        | Likely folder                                                                                  |
| ---------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| Wrong life total / wrong damage                | `replacement/` (prevention/redirection), `combat/`, `ability/effects/damage_*`                 |
| Trigger fires when it shouldn't / doesn't fire | `trigger/`, `game_loop/trigger_handler.rs`, `staticability/static_ability_disable_triggers.rs` |
| Continuous effect (anthem, protection) wrong   | `staticability/` + `staticability/layer.rs`                                                    |
| Cost paid incorrectly                          | `cost/`, `game_loop/cost_payment.rs`, `mana/`                                                  |
| Spell resolves wrong                           | `ability/effects/<api>_effect.rs`                                                              |
| Attack/block illegal or missing                | `combat/attack_constraints.rs`, `combat/attack_restriction*.rs`                                |
| Zone change misroutes                          | `replacement/replace_moved.rs`, `ability/effects/change_zone_effect/`                          |
| Wrong card available to play                   | `game_loop/playability.rs`, `card/card_play_option.rs`; run with `FORGE_CARD_TRACE=<name>`     |

## Conventions

- **Mirror Java exactly.** See `docs/agents/PARITY_PHILOSOPHY.md`.
- **One Forge concept per file.** New effects, triggers, replacements, statics, keywords each get their own file named after the Java class, registered in their module's `mod.rs`.
- **`Game` owns everything.** All mutation flows through `&mut Game`. No interior mutability, no shared references.
- **`GameState::cards` holds `Arc<Card>`, so a snapshot shares every card and a card is copied on its first write after one.** `GameSnapshot::capture` runs before every spell cast and every activated ability's cost payment, and it clones the vector of pointers, not the cards. Write through `card_mut`, or `Arc::make_mut` in a loop over `cards`. A loop that writes every card copies all of them while a snapshot is alive, so test each card first and write only the ones that change, as the layer pass's reset does with `static_layer_reset_is_noop`.
- **Engine hash collections use the fixed-hasher aliases.** `crate::HashMap` and `crate::HashSet` (`manabrew-engine/src/lib.rs`) wrap the std collections with `DefaultHasher`, so iteration order is the same on every run of the same seed. `manabrew-engine/clippy.toml` rejects `std::collections::HashMap`, `HashSet`, `RandomState`, and `rand::thread_rng`. Where iteration order reaches game logic, use `IndexMap`/`IndexSet`.
- **Crate-wide `#![allow(...)]`s in `lib.rs` are intentional.** They document parity trade-offs.
- **A trigger pushed onto a built card must raise `base_trigger_count`.** The layer reset in `staticability/layer.rs` truncates `card.triggers` to `base_trigger_count + pump_trigger_count` on every pass, so a trigger added to an effect card after construction vanishes on the first zone move unless the count covers it. `effect_effect.rs` and `player/player_factory_util.rs::add_trigger` both set it; do the same in any new code that builds an effect card.
- **A keyword that changes a card's types does it inline in the layer pass, not through a generated static.** `keyword_gen.rs` makes triggers and abilities but no static abilities, and `StaticAbility::remove_type` is parsed from `RemoveType$` and then never read, so a `RemoveType$` written into a card script is silently a no-op. Keyword-derived type and restriction changes live in the "Keyword-derived restrictions" block of `staticability/layer.rs` (Unleash's can't-block, Impending's `RemoveType$ Creature`), which runs after the per-pass reset so it recomputes every pass and reverts on its own. Anything there that drops a card type must call `sanitize_subtypes` itself, as the `type_changed` loop at the end of the pass does — that loop only covers targets the static effects touched, so an inline change skips it and leaves creature subtypes on a card that is no longer a creature.
- **A trait on a card face carries that face's `CardState`, and the other part's traits need it too.** Java's `CardTraitBase.getCardStateName` returns the state the trait was defined on, and `TriggerUnlockDoor.performTest` compares it against the unlocked door with no null guard — so a `ThisDoor$ True` trigger always knows its face. In this port `card_assembly::mark_triggers_card_state` marks the front card's copy of both faces' triggers, but `Card::transform` swaps in `other_part.triggers`, a separately parsed list. Those have to be marked at assembly as well, or after a Room is cast as its right half its `ThisDoor$` trigger has no state, the face check is skipped, and it fires for both doors. Anything else read off a trait's `CardState` needs marking in both places.
- **A Room keeps both doors' triggers on the card and filters them by which doors are open.** Java moves `currentState` and registers that state's traits (`Card.updateRooms`); this port has no per-state trait lists at runtime, so `card_assembly` puts both faces' triggers on the front card with their `CardStateName` and `TriggerHandler::register_one_trigger` skips one whose `LeftSplit`/`RightSplit` door is still locked (`Card::room_door_locked`, backed by the `UnlockedDoors` SVar). `Card::transform` therefore restores the combined trigger list for a Room after its swap, next to the `UnlockedDoors`/`UnlockedRoomCount` SVars and the unlock abilities it already carries across — casting a Room as its right half would otherwise leave the left door's triggers on `other_part`, where no later unlock can reach them, and a card like Defiled Crypt would never fire its half once opened. Static abilities and replacement effects still swap, because nothing filters those by door; a Room whose locked half carries one needs that filter written first.
- **An `ST$` ability resolves where it is played and never reaches the stack.** Java's `MagicStack.add` (`MagicStack.java:327`) calls `AbilityUtils.resolve(sp)` and returns for an `AbilityStatic`, before it can become a stack entry. The port mirrors this case by case in `GameLoop::play_spell_ability`, which routes Plot, `Mode$ TurnFaceUp` and a Room's unlock to `resolve_immediate_ability`; a new `ST$` API needs the same routing or it buys its controller a priority round Forge never grants, which shows up as `PASS_STATE` rather than a state divergence. The Room unlock is synthesised as `AB$ UnlockDoor` in `card_assembly.rs` where `CardFactoryUtil.abilityUnlockRoom` writes `ST$`, so the test is `ActivatedAbility::is_unlock_door`, not the prefix. Java's second half of that branch, a trigger whose overriding ability is an `AbilityStatic`, is not ported.
- **A face-down permanent has a face-down state.** `Card::set_original_state_as_face_down` (Forge: `setOriginalStateAsFaceDown` + `CardUtil.getFaceDownCharacteristic`) saves the printed characteristics in `face_down_state` and leaves a colourless 2/2 `Creature` with no cost and no abilities; `turn_face_up` restores them, and every path that turns a card up or moves it between zones goes through `turn_face_up`. `card_name` is kept, so name lookups still work; the parity snapshot and `FmtCtx::card` blank it, and a seat's view sends it only to a player `game_view_dto::can_face_down_be_shown_to` lets see the face. Game-log text goes to every seat, so it names a face-down card by `Card::log_name`, `FACE_DOWN_LOG_NAME` (Forge's log prints `CardView.toString`, "Face-down card (H<id>)"); turning a morph face up still logs its name, since that special action reveals it. The face-down cast (Morph, Megamorph, Disguise) applies it before the permanent enters, as `abilityCastFaceDown.resolve` does, so enters-the-battlefield replacements see the face-down card. The action space prices that cast on a face-down copy of the card (`playability::can_pay_face_down_cast`), as `CostAdjustment.adjust` turns the host face down before it reads the cost statics: `ValidSpell$ Spell.isCastFaceDown` (Obscuring Aether, Dream Chisel) and a `ValidCard$ Creature` static adjust the {3} or a `MayPlayAltManaCost$`, and a static keyed on the printed colour or types does not. `CantBeCast` is asked twice, as Forge does: the action space asks it of the card as it sits in hand, face up (the harness's `ActionSpace.spellHost` is an LKI copy of the hand card), for the land and `MayPlay$` face-down options as well; the cast asks it again of the face-down card on the stack (`checkRestrictions` after `moveToStack`), so a face-down cast under Void Winnower is offered and then rolled back. That second check runs for every spell, and its `OnlySorcerySpeed` test ignores the pending cast's own stack entry, since Java's `MagicStack` gets the spell only at `add`. Java's `GameActionUtil.rollbackAbility` leaves `Card.facedown` set on the card it puts back in hand after a failed face-down cast; Forge's `PlaySpellAbility.playSpellAbility` clears it afterwards, but the harness calls `rollbackAbility` directly. Every later LKI copy of that card is then face down (`CardCopyService.getLKICopy`): no name, a `Creature` of mana value 0 with no abilities, keeping its face-up colour and power and toughness, so each later `CantBeCast` check on it sees mana value 0, and `CostAdjustment` prices a later face-down cast on the face-up card because the host already reads as face down. Rust mirrors this only when `GameState::mirror_forge_bugs` is set, which the parity runner does and the engine default does not: the cast rollback sets `Card::stale_face_down` on the hand card, `static_ability_cant_be_cast::restriction_host` asks `CantBeCast` of the face-down view, and `playability::can_pay_face_down_cast` prices the face-up card. Any zone move clears it, as Java's `copyCard` does not copy `facedown`; Java keeps it on a move to the battlefield, which is not mirrored. Manifest, Manifest Dread and Cloak apply it before the card moves, as `Card.manifest` and `Card.cloak` do; Cloak's Ward:2 is an intrinsic keyword of the face-down state, not a pump keyword that cleanup removes. A `FaceDown$` ChangeZone, ChangeZoneAll or Dig applies it before the move as well, through `card_factory_util::turn_face_down_with_state` (Java `turnFaceDown(true)`: the state and the Morph or Disguise turn-up ability) and `set_face_down_state` (`CardFactoryUtil.setFaceDownState`: `FaceDownPower$`, `FaceDownToughness$`, `FaceDownSetType$`); MakeCard's `FaceDown$` calls the first alone, as `MakeCardEffect.finishMaking` does. A manifested or cloaked creature card gets a `Mode$ TurnFaceUp | ManifestUp$ True` (or `CloakUp$`) ability for its mana cost from `card_factory_util::ability_turn_face_up` (Forge: `CardState.getManifestUp`), pushed before the move as the morph cast pushes its own; cost reductions for turning face up (`ValidSpell$ Static.isTurnFaceUp`) are not applied yet. Turning face up restores the card first and runs the `TurnFaceUp` replacement after, behind a `cant_happen_check` (Forge: `Card.turnFaceUp`), and its `ReplaceWith$` ability runs through `replace_moved::execute_replace_with`. A `Duration$ UntilFacedown` (Vesuvan Shapeshifter's copy) goes on `GameState::facedown_commands` for its host (Java `addFacedownCommand`). `SetState`'s `TurnFaceDown` runs them before it captures the face-down state, which is taken from the current characteristics, so an unclone run after it would overwrite the 2/2; a host's commands are dropped when it leaves the battlefield, as Java's belong to the old object.
- **A token's edition follows `TokenInfo.getProtoType`, and its art pick draws from the game RNG.** `EffectContext::sync_token_art_rng` starts from the ability's original host (else its host), follows `TokenFallbackCode`, and pins the script's edition for the rest of the game (`GameState::token_edition_pins`). An edition without the token falls back to the first edition, in code order, that has it (`TokenDb.getTokenFromEditions`); more than one art variant draws `nextInt(variants)`. The token takes that edition as its set code and an effect card takes its host's, as in Forge; other cards made during the game have no set code and go straight to the fallback.
- **A trigger's player filter goes through `player_property::is_valid`** (Java `Player.isValid`), from `Trigger::matches_optional_valid_player_filter`. The compiled-selector player matcher in `valid_filter.rs` has no game, so it cannot answer `Active`, `NonActive` or any other property that needs one and counts them as matches. `valid_filter::matches_valid_player_selector_in_game` keeps that fast path for a filter it can answer (`You`, `Opponent`, `Player`) and sends any other to `player_property::is_valid`; the `Caster$` of `CastWithFlash`, `ValidPlayer$` of `CantDraw`, `ValidActivator$` and `ValidTarget$` of Panharmonicon and `DisableTriggers`, `ValidTarget$` of `InfectDamage`, the player `Target$` of `CantAttack` and a `ConditionPresent$` player use it, so Unstable Glyphbridge's `Target$ Player.IsRemembered` no longer names every player. The callers with no game still count those properties as matches: `CardTraitBase::matches_valid_with_player`, the card-slice statics (`CantPutCounter`, `BlockRestrict`, `IgnoreHexproof`), the attack and block costs, and the `Untap` and `AddCounter` replacements. A spell-cast trigger reads the caster from `RunParams::activator`, which every `SpellCast` emitter sets, as `MagicStack.add` puts `AbilityKey.Activator`.
- **A static ability only applies when its conditions hold, so every reader goes through `staticability/`.** `StaticAbility::check_mode` answers the mode alone; `check_conditions_full` is the port of Java's `StaticAbility.checkConditions` and is what gates `Condition$`, `CheckSVar$`/`SVarCompare$`, `Phases$`, `PlayerTurn$`, the host's zone and `phased_out`. A combat or playability path that matches a static itself — mode plus `ValidX$` — applies it unconditionally, and a card that is only sometimes unblockable is then always unblockable. `combat_util::can_creature_block` had such a copy of `CantBlockBy` and now calls `static_ability_cant_attack_block::cant_block_by`. A `CantAttack` static is answered per attacker and defender by `static_ability_cant_attack_block::cant_attack` (Java `CombatUtil.canAttack(attacker, defender)`), from `combat_util::get_available_attackers`, `can_attack_defender` and `can_attack_next_turn`; the layer pass used to fold it into a per-card flag over the static's `ValidCard$` set, which could not read `Target$` (The Aetherspark forbids attacking only itself). The Defender keyword is answered in `cant_attack` too, since Java makes it a `CantAttack` static whose one exception is a `CanAttackDefender` static (`applyCantAttackAbility`); `combat_util::can_attack_defender` consults `CanAttackDefender` nowhere else, and requiring one for every player defender kept any creature from attacking a player whose opponent controlled a planeswalker. `CantBlock` still goes through the layer pass's `cant_block_static` flag, which is equivalent because Java's `cantBlock` has no defender dimension. Read the `static_ability_*.rs` function before writing a match loop; if one exists, call it. The parity agent (`deterministic_agent.rs`) has its own copies of several of these against the snapshot game, so a fix here can measure zero until the agent's copy is fixed too. Its `choose_attackers` draws each attacker's defender from the flat `get_possible_defenders` list, where Java's `DeterministicController` draws from that attacker's `CombatUtil.canAttack` list, so parity never observes `can_attack_defender`.
- **A `MustAttack$` static names its defenders through `get_defined_players` and `get_defined_cards`, as Java's `getDefinedEntities` does.** `static_ability_must_attack::entities_must_attack` returns a player or a card (Gideon Jura's `EffectSource`), and `attack_requirement::compute_attack_requirements_with_defenders` counts them the way Java's `AttackRequirement` constructor does: a named entity adds to that defender only, an unnamed requirement or a goad adds to every possible defender. `resolve_defined_players` answers a key it does not know with the activator, where Java's `getDefinedPlayers` answers every player and then filters, so a Java branch missing from `resolve_defined_players_with_sa` names the controller without saying so; `EffectSource*`, `Enchanted*`/`Equipped*` with `Controller` or `Owner`, and `CardOwner` have their branches. The declare-attackers loop in `combat_phase.rs` counts only must-attackers left out of the declaration, not one sent at the wrong defender, and `AttackConstraints` (the port of Java's solver) is not called by the game loop.
- **An attacker that leaves the battlefield stays in `CombatState::attackers`** until `remove_absent_combatants` prunes it after the declare-blockers priority round. Code that reads attackers in the middle of a step checks the card's zone, as the Sneak offer in `playability.rs` does; a creature returned to hand by Sneak's cost is still found by `get_defender_by_attacker` when the Sneak spell resolves.
- **A combat relation in a card filter reads `GameState::turn.combat_block_assignments` when the filter has no combat.** Java's `CardProperty` reads `game.getCombat()`; here the `CombatState` lives on `GameLoop`, and only the cast path hands one to `MatchContext`, so an effect's `ValidCards$`, a trigger's `ValidCard$` and targeting saw no blocks at all: `blockingSource`, `blockedBySource` and `blocking<Defined>` never matched (Baneclaw Marauder's -1/-1, Tinder Wall's target). `valid_filter::combat_blocks` takes the combat when there is one and the game's record otherwise. The record is written at declare blockers, cleared at end of combat, and pruned only by `CombatState::remove_from_combat`, so a blocker that left the battlefield keeps its relation, which is how Java's combat LKI answers `blockedBySourceLKI` and a dies trigger's `blockingSource`. The other combat properties Java reads (`attackingSame`, `attackingYouOrYourPW`, `attackedBySourceThisCombat`, `attackersBandedWith`, `blockedThisCombat`, `blockedByRemembered` and kin) are still lowered to a subtype and never match.
- **A player's poison, energy and rad counters are `PlayerState` fields; every other counter type lives in `PlayerState::counters`.** Java keeps one map (`GameEntity.counters`), so a stun counter can land on a player (Homesickness's `Defined$ ParentTarget` walks past a Tap that targeted nothing to the Draw's target player). List, count and add player counters through `GameEntityCounterTable`, which reads both. `get_defined_entities` asks for players as well as cards for `Targeted` and `ParentTarget`, because Java's `getDefinedEntities` always does; other card references ask only when no card was found, since `resolve_defined_players_with_sa` falls back to the controller for a name it does not know.
- **`Defined$ Triggered<Key>Controller` works for any triggering-object key**, as the `endsWith("Controller")` branch of `AbilityUtils.getDefinedPlayers` does: `resolve_defined_players_with_sa` answers its named cases first and then parses `<Key>` with `ability_key::from_string`. Before that, `TriggeredAttackerController` and `TriggeredBlockerController` fell through to the controller, and Marchesa's Decree drained its own controller.
- **Forge meets block requirements in the blocking controller, not in `PhaseHandler`.** `declareBlockersTurnBasedAction` adds no blocks; `InputBlock` and `AiBlockController` make the required ones, and the parity `DeterministicController` only narrows each blocker's options with `CombatUtil.canBlock(attacker, blocker, combat)`. The engine's auto-assign after `choose_blockers` runs only for agents whose `enforces_block_requirements` is true (`PromptAgent`); the parity agent filters each blocker's options with `combat_util::lure_forbids_block` over the blocks chosen so far.
- **`PromptAgent`'s combat prompts come from the engine's own checks.** `snapshot_state` keeps a clone of the game while the agent's seat declares attackers or blockers (`combat_game`), and `agent_impl/combat.rs` builds the prompt from it. Each attacker's targets are its `combat_util::can_attack_defender` defenders; `mustAttack` and `mustAttackTargetIds` come from `compute_attack_requirements_with_defenders`, the targets being the legal defenders with the highest requirement count. Each attacker's blockers pass `can_creature_block` and not `lure_forbids_block` over an empty declaration, as Java's `canBlock(attacker, blocker, combat)` answers at the start of `InputBlock`; `minBlockers` and `maxBlockers` come from `get_min_max_blocker`, and `blockRequirements` lists each blocker's `compute_must_block_targets`, the blocks the `enforces_block_requirements` pass would add. `PromptInput::validate_response` rejects an attack or block pair the prompt did not offer (`IllegalAssignment`), so the client is asked again instead of losing the pair. The engine still checks neither `can_attack_defender` on a declared pair nor min and max blockers (menace) at declaration, and `compute_must_block_targets` makes a "must be blocked if able" attacker a requirement of every able blocker, where Java's is met by one block.
- **Every `chooseAction` option id names one engine action.** `PromptAgent::choose_action` appends `#<alt_cost_index>` to a cast id when the index is not 0 (two Evoke costs, a card's `MayPlay` grants), drops an action whose id is already listed (the engine can list the same `PlayOption` twice), and resolves a cast answer by exact id.
- **`ChangesZoneAll` fires from a `CardZoneTable`.** Moves record into `GameState::pending_change_zone_table` while one is open. Stack resolution, a permanent spell entering (`PermanentEffect`) and each state-based-action pass (`state_based_actions_pass`, up to nine passes as in `GameAction.checkStateEffects`) open one and end with `trigger_changes_zone_all`. A move outside those needs its own table, or "one or more" triggers never see it.
- **A damage replacement can move the damage to another entity.** A `DamageTarget$` replacement whose `ReplaceEffect` sets `VarName$ Affected` (`VarType$ Card` or `Player`) rewrites the event between `DamageToPlayer` and `DamageToCard` and returns `Updated`, so the next pass collects the new target's replacements (`set_replacement_event_affected`). Combat damage and `DamageResolve` replace the whole damage map in one pass, `replacement_handler::run_replace_damage` (Java `ReplacementHandler.runReplaceDamage`, called once from `GameAction.dealDamage`): every event is filed under each replacement that applies to it, by target controller in seat order; the first player with candidates picks one replacement, which runs on all of its events, and an `Updated` event goes back only to the replacements that have not run on it. Combat records attackers' damage and then blockers' before that pass, as `Combat.assignCombatDamage` does, and `GameState::add_damage_after_prevention` deals the replaced entries, so history, deathtouch and toxic name the entity actually damaged. `DealDamage`, `DamageAll`, `Fight` and cost damage go through `GameState::deal_damage`, which builds a one-entry damage map and runs the same pass, so the replacement is chosen per event rather than once per resolution in APNAP order. A replacement whose `ReplaceWith` is not a `Replace*` effect (Anti-Venom's counters, The Mindskinner's mill) is marked Replaced and buffered; `execute_replace_damage_buffered_sa` runs it at the end of the pass, before the damage is dealt, grouped by `ExecuteMode` and reading each event's final target and amount (`executeReplaceDamageBufferedSA`); `ReplaceCount$` and `Defined$ Replaced<Key>` resolve in its SVars. An event whose source a `CantPreventDamage` static covers is filed as `NoPreventDamage` (`static_ability_cant_prevent_damage::cant_prevent_damage`, Java `Card.canDamagePrevented`): a `Prevent$` or `PreventionEffect$` replacement on it returns NotReplaced after its `Optional$` prompt, and its buffered ability still runs, on the whole amount with `AlwaysReplace$` and on none without (Sunspine Lynx with The Mindskinner deals the damage and mills). Not ported: a cause with `NoRedirection` or `NoPrevention`, since `run_replace_damage` takes no cause, `ReplaceSplitDamage`, `NextN`/`DivideShield` shields, and the per-event callers' triggers and history, which still name the original target.
- **An enters-tapped replacement has a second, hand-rolled path.** After the `Moved` replacement chain, `staticability::layer::apply_etb_tapped_with_agents` taps a card the chain left untapped when a battlefield permanent has a `ReplaceWith$ ETBTapped` replacement whose `ValidCard$` matches it. It matches with the compiled selector, as Java's `matchesValid` does; it used a hand-rolled `CardFilter` that read `OppCtrl`, `Artifact` and `nonBasic` as subtypes, so Authority of the Consuls, Dauntless Dismantler, Spider-Woman and Zhao never matched there. The path ignores the replacement's conditions (`IsPresent$`, `CheckSVar$`), and no script uses the `Mode$ ETBTapped` static its first pass reads.
- **Damage to a player loses life through `simultaneous_damage`.** `player_deal_damage` adds the life lost to `PlayerState::simultaneous_damage`; `GameState::lose_life_simultaneously` (the tail of Forge's `GameAction.dealDamage`) drains it and fires `LifeLost` per player and then `LifeLostAll`. It runs after every effect in `resolve_effect_once`, in `fire_combat_damage_triggers`, and at the `DamageYou` cost sites. A new damage path outside those three needs its own call.
- **`Mode$ TapAll` fires once for a whole batch of cards that became tapped together, not once per card — and the batch has to actually be collected at every place creatures get tapped.** Java's `TriggerTapAll.performTest`/`setTriggeringObjects` both read the plural `ValidCards$` against `AbilityKey.Cards` (a list); the port's `TriggerTapAll::parse` read the singular `keys::VALID_CARD` instead of `keys::VALID_CARDS`, so the filter was always `None` and the trigger matched unconditionally once anything reached `perform_test` at all — invisible as long as nothing populated `params.cards`, which is what let the key-name bug hide behind a second, independent gap: `combat_phase.rs`'s declare-attackers step tapped every attacker and fired the per-card `Taps` trigger for each, but never batched them into one `TapAll` firing the way Java's `PhaseHandler.declareAttackersForPlayer` does (collecting every newly-tapped attacker into one `Cards` list after the loop) — so `Mode$ TapAll` triggers never fired from combat at all. Fixing the missing firing site alone surfaced the key-name bug immediately (a card whose `ValidCards$` should have excluded its own tokens started matching everything); fix both together or the first fix alone regresses. `cast_spell.rs`'s convoke/improvise tap loop had the same per-card-instead-of-batched shape and needed the same correction.
- **Where Java draws `nextBoolean()`, draw `next_boolean()`; `next_int(2) == 0` is its negation.** In `java.util.Random` both read the same top bit of the state, so `nextInt(2) == 1` is `nextBoolean()` and the draw count is identical either way — the only symptom of getting it wrong is every coin landing the opposite side, which the RNG-count comparison cannot see. `flip_coin_effect.rs` had it inverted; the `GameRng` trait default is `next_int(2) == 1` so a non-Java adapter agrees too.
- **A static's affected set is taken at its first layer, after the lower layers apply.** Java `GameAction.checkStaticAbilities` walks the layers in order (CR 613.6), so a layer-7 `Ninja.YouCtrl` sees a layer-4 type change and a control change made earlier in the pass. `staticability/layer.rs` sorts the statics by their first layer (`first_static_layer`) and stages each static's effects; before a static runs, every staged effect ahead of it (a lower layer, or the same layer with a lower number) applies, so a static in a layer sees the ones applied before it (`GameAction.checkStaticAbilities`). Within a layer statics are numbered characteristic-defining first, then by `Card::layer_timestamp` (Java `getLayerTimestamp`, CR 613.7): it is set with the zone timestamp on every zone entry and again from the same counter (`GameState::next_timestamp`) when the card is attached (CR 613.7e), so an Aura or Equipment attached later applies after a creature's own static in that layer. Java's dependency reordering (`findStaticAbilityToApply`, CR 613.8) is not ported. A `RemoveAllAbilities$` static clears `granted_keywords` when it applies, so keywords granted by statics applied before it go (Java `KeywordsChange` clears the list at its timestamp, `Card.changedCardKeywords` keyed by timestamp); its own `AddKeyword$` is staged after its RemoveAll, so the static keeps what it grants (Spider-Man No More's Defender). Every `pump_keywords` instance carries the timestamp of the effect that granted it (`Card::add_pump_keyword`, drawn from `GameState::next_timestamp`, the counter layer timestamps come from), as Java keys `addChangedCardKeywords` by timestamp (`Card.java:5025`). A RemoveAll static sets the older ones aside into `Card::pump_keywords_removed_by_statics` for that pass (`KeywordCollection::take_older_than`), the reset at the start of the next pass puts them back, and `Card::remove_pump_keyword` removes from both; before this a pump's keyword outlived a newer RemoveAll, and a creature given indestructible by Horrid Vigor kept it under Noggle the Mind and survived its damage. A new effect kind has to be placed in `classify_static_layers`, or it is staged against the wrong layer.
- **`AffectedDefined$` picks a continuous static's candidate set; `Affected$` only filters it.** Java `StaticAbilityContinuous.getAffectedCards` starts from `getDefinedCards(host, AffectedDefined)` (phased-in only) when the key is present, and from the battlefield or `AffectedZone$` otherwise; `Affected$` is applied to whichever set that was. Forge #11932 (`3d422bc9f34`, in the `e7d2b93ba` submodule bump) rewrote 2,411 scripts to this form — `Affected$ Creature.EnchantedBy` became `AffectedDefined$ Enchanted | Affected$ Creature`, and 1,067 of the lines carry no `Affected$` at all. `layer.rs` read only `Affected$`, so an aura pumped every creature, and a line with no `Affected$` fell to the `"Creature.YouControl"` default and granted its keyword to every creature you control: 292 of 720 meta games regressed on P/T alone. The layer pass now takes the `affected_defined` branch first, as Java does, and `ability_utils::get_defined_cards` resolves `AttachedBy <v>` (the card each `<v>` is attached to, `AbilityUtils.java:106`) beside the bare `Enchanted`/`Equipped` tokens. A key the IR already carries and the MayPlay path already read (`static_ability_continuous::can_play`) was still unread on the path that mattered — grep the key at every consumer, not the first one you find.
- **A trigger that filters on a `RunParams` field is dead until some firing site fills that field, and the trigger's own file will never show it.** Three separate bugs this session were this one shape. `Mode$ TapAll` read the wrong key name _and_ had no batching firing site (`131032d0c`); `Mode$ DiscardedAll` fired per card instead of per batch (`f361e16d2`); `Mode$ Attacks | Attacked$` filtered on `params.attacked_player`/`attacked_card`, which `combat_phase.rs` never set — it set only `defending_player`, so every `Attacked$` test saw `None` and failed (Java keeps these as two separate keys: `CombatUtil.checkDeclaredAttacker` puts the defender entity under `AbilityKey.Attacked` and the player under `AbilityKey.DefendingPlayer`). In all three the trigger file itself mirrored Java's `performTest` correctly and the filter compiled to a live predicate, so reading either proved nothing. **Grep every assignment of the field before reading anything else**; if the only hits belong to other trigger types, the trigger cannot fire. `Mode$ Taps | FirstTime$` had the same shape, since no `Taps` site set `RunParams::first_time`; rather than touch the scattered firing sites, `GameState::tap` counts `Card::tapped_this_turn` (reset at cleanup and on entering the battlefield) and `TriggerTaps` reads that count when the site did not set the flag. Every `Taps` site taps before it fires, so the count is 1 on the first tap.
- **A mana ability is decided by the whole `SubAbility$` chain, not the root `AB$`, and `parse_activated_ability` cannot see the chain.** Java's `SpellAbility.isManaAbility` (CR 605.1a) walks every link for a mana part, and disqualifies the ability when the root cost or any link moves a card to or from a library — `LibraryMovementCostVisitor` for the cost, a `movesCardToOrFromLibrary` override per effect class for the links. The port answered from the root's own text alone, because `parse_activated_ability` is handed one ability string with no card and no SVars, and a `SubAbility$` is an SVar name. That got it wrong in both directions: Foraging Wickermaw (`AB$ ChooseColor` → `DBMana` = `DB$ Mana`) was not a mana ability, so `get_activatable_abilities` offered it at priority where Forge only ever offers it while paying, and Chromatic Sphere (`AB$ Mana` → `DB$ Draw`) was one when Forge says it is not. 28 cardsfolder cards were misclassified, 20 one way and 8 the other. `Card::classify_mana_abilities` does the walk and is called from the end of `card_assembly`, **not** `Card::new`: `Card::new` never receives SVars (the caller merges each face's in afterwards), so running it there silently walks an empty map and measures nothing — the same trap `add_etb_keyword_replacements` is positioned to avoid. Java's answer is fixed by the card script rather than the board, so once at assembly is the right frequency. `spell_ability_effect::moves_card_to_or_from_library` collects Java's per-effect overrides into one `ApiType` match, because the port's `SpellAbilityEffect` trait is compile-time dispatched and has no instance to ask; keep it in sync when an effect gains or loses the Java override.
- **`Mode$ DiscardedAll` fires once per player for everything they discard in one event, not once per card.** Java's `SpellAbilityEffect.discard`, `CostDiscard.doListPayment`/`doPayment` and `PhaseHandler`'s CLEANUP hand-size step each collect a player's discards for the event into one `CardCollection` and fire `TriggerType.DiscardedAll` once with the whole list as `AbilityKey.Cards`; the key itself is singular (`ValidCard$`), unlike TapAll's plural `ValidCards$` — checked directly against Java source, not assumed parallel. `action.rs::discard_card` used to fire it per card. `GameState::pending_discard_batch` (`begin_discard_batch`/`end_discard_batch`, next to `discard_card`) gives a caller that discards more than one card for one event a way to batch them; `begin` is idempotent (a nested call joins the outer batch) and `end` no-ops if nothing is open, so wrapping a loop that might discard zero, one or many cards is always safe. Every call site that can discard more than one card for one event is wrapped: `discard_effect.rs`'s per-player loop (`DB$ Discard`, all its sub-modes), both cost-payment discard paths (`game_loop/cost_payment.rs`'s interactive payment and its "auto"/AI-simulation twin in `ability/effects/cost_payment.rs`), and the cleanup-step hand-size discard in `game_loop/phase_handler.rs`. A site with an early-return before any card is actually discarded (a validation failure mid cost-payment) closes the batch on that path too, or a later unrelated discard on the same turn would wrongly join an abandoned batch that never fires. `recruit_effect.rs`'s discard is unbatched on purpose: Java's own `RecruitEffect.resolve` calls the batching `discard()` helper once per target player with that player's (always ≤1-card) list, which is exactly what the port's unbatched fallback already produces.
- **A cost part's type filter treats `;` as an OR between independent filters, mirroring Java's `CostPartWithList.getType().split(";")`.** The compiled selector only understands `,` that way — `parity selector "Creature;Land"` shows the whole string collapsing into one dead `Subtype("Creature;Land")` predicate — so a cost filter has to split on `;` before it ever reaches `cached_compiled_selector`/`matches_valid_card_selector_*`. Most cost types already do, either because they route through `matches_change_type` (`ability_utils.rs`, which self-splits on `;`) or because their own candidate-finder does `type_filter.split(';').any(...)` by hand (`get_tap_type_targets`, `get_zone_targets`, `is_excluded_as_source`, `matches_exile_from_stack_filter`, `cost_remove_any_counter::valid_cards`). `CostPart::AddCounter`'s `candidates` (`cost_put_counter.rs`) and `CostPart::SubCounter`'s `get_sub_counter_targets` (`cost/mod.rs`) compiled the raw, unsplit filter instead — fixed to match the established pattern. A new cost-part filter reader should call one of the existing split-aware helpers rather than reaching for `cached_compiled_selector` directly. Sacrifice and return costs (`get_sacrifice_targets`) match each alternative against the paying ability, as `CostSacrifice` does; `matches_change_type` has no ability and accepts any `cmc<op><SVar>`, so a filter that reads an SVar must not go through it.
- **`ConditionDefined$` and a `Defined$ <ref>.<valid>` filter read the object Java's `getDefinedCards` returns, not the live card.** Java hands out a target through `Game.getChangeZoneLKIInfo` (`AbilityUtils.java:146`) and a `Triggered<X>LKICopy`, `ThisTargetedCard`, `ParentTarget` or `*LKI` reference as the object it recorded (`AbilityUtils.java:166`), so a card a preceding sub-ability moved still reads its old state. `spell_ability_effect::get_defined_card_objects` resolves the ids and answers those references with `GameState::get_change_zone_lki_info`, the card as it was when it left the battlefield; `condition.rs` matches `ConditionPresent$` against these objects, and the `<ref>.<valid>` branch of `resolve_defined_cards_for_sa` filters them. Before this, a card an earlier link exiled or bounced was matched live: Driftgloom Coyote's `Creature.powerLE2` read the exiled creature's printed power instead of its pumped one, and only a bare `tapped` had an LKI answer (Brackish Blunder). `TriggeredNewCardLKICopy` stays live, because `NewCard` is the object the move created and the table holds the one that left. A target is looked up by the zone timestamp it was targeted with (`SpellAbility::target_zone_timestamp`, which reads the node's own target or the `(card, zone timestamp)` pairs `chain_target_cards` carries down the chain) through `get_change_zone_lki_info_at`, as Java keys the table by `(id, gameTimestamp)`: a card targeted in a graveyard after it died earlier in the same window reads the graveyard card, and a target that left and came back reads the object that left. Only a node's first target records its timestamp; any other reference asks `get_change_zone_lki_info`, which answers the LKI whenever the card is off the battlefield. The table is cleared when the stack empties, so a reference read after that (a delayed trigger's `DelayTriggerRememberedLKI`) reads the live card again. `Card::lki_power` and `lki_toughness` outlive the table and stay on a card through its later moves, so `ability_utils::handle_paid` reads them only for a list of recorded objects (paid costs, cards that entered this turn); a `Remembered$` list reads the live card, as Java's `calculateAmount` takes it through `getCardState`, and Lie in Wait's `Remembered$CardPower` had dealt a returned creature's power from when it last died. Past its list-level branches `handle_paid` folds `svar::card_x_property` (Java `xCount`) over the cards with `svar::list_property_fold`, as the tail of Java's `handlePaid` does; it used to count the cards instead, so Stormscale Anarch's `Discarded$CardMulticolor.4.2` dealt 1. It also holds the card at the moment it moved, where Java's entry is the `lastStateBattlefield` copy (`GameAction.java:184`), so a change made earlier in the same resolution shows in the Rust LKI and not in Java's.
- **A Modal DFC's back face is castable from hand as a spell, not just as a land.** `BackFaceLand` already covers a land back face (Pathways); a non-land one (Peter Parker // Amazing Spider-Man) needed its own check, mirroring `Card.collectSpellAbilities`'s `isModal() && hasState(Backside)` — unconditional, unlike `CardStateName::Secondary` (Adventure/Omen), which is only ever offered from the `Original` state. `PlayCardMode::Secondary` doubles for both: a card only ever has a `Secondary` state or a modal `Backside`, never both, so `playability.rs` checks `Card::is_modal()` before offering the option and `cast_spell.rs` reads `other_part.state_name` off the card itself to know which one to build, rather than adding a new wire-visible `PlayCardMode` variant (that would also touch `manabrew-protocol`'s mirror enum and the UI). The back face is routinely a vanilla creature with no `A:` line — `build_spell_ability_for_host_cast` returns `None` for that, so `build_spell_ability_for_card_state_cast` needed the same vanilla-spell fallback `build_spell_ability_for_card_cast` already had for the front face; factored into `build_vanilla_spell_ability`.
- **`DB$ Connive` draws and discards even when its conniver has already left the battlefield; only the +1/+1 counter is gated on it still being there.** Java's `ConniveEffect.resolve` checks the battlefield only around `conniver.addCounter(...)`, not around the draw or the discard prompt — a triggered Connive whose host gets sacrificed by the legend rule before the trigger resolves (Prowler, Clawed Thief connives when another Villain enters, and casting a second Legendary copy sacrifices the original conniver first) still draws and discards, it just gains no counter. The zone check belongs only around the counter placement at the end of `connive_effect.rs`, not as an early return. A **targeted** Connive (`TargetMin$ 0`, "up to one target creature") is a different case: no target chosen there means no conniver at all (Java's `getTargetCards` returns empty and `resolve` returns immediately), so a targeted or `Defined$` Connive goes through `get_target_cards`, and only one with neither falls back to `sa.source`. That fallback stands in for Java's `Self`, which is the host object even after it moved; the `SelfCard` arm here answers nothing for a trigger whose host changed zones, so Prowler's implicit `Self` would lose its connive. Each conniver runs the `Connive` replacement first (`replace_connive.rs`, Leader, Super-Genius), and a replaced one skips its draw and discard.
- **`K:etbCounter`'s fourth field is appended onto the replacement's own param map, so a `ValidCard$` there overrides the base `Card.Self` clause rather than gating on presence.** `CardFactoryUtil.makeEtbCounter` builds `"Event$ Moved | ValidCard$ Card.Self | ... " + extraparams`, and Java's param map keeps the last value for a repeated key — so `extraparams$ ValidCard$ Card.Self+escaped` REPLACES the base clause, and the counters apply only when that more specific filter matches. `move_card_internal`'s inline `K:etbCounter` parser reads the fourth field through `CardTraitRequirementsIr`, which only recognizes `IsPresent$`/`CheckSVar$`-style keys — a `ValidCard$` clause fell into its `_ => {}` arm and was silently satisfied, so escape/`wasCastByYou`-gated counters applied on every entrance, not just the one Java names. The extra field can carry either shape (or, for a Saga-style card, neither); check for `ValidCard$` first and match it against the card directly before falling through to `CardTraitRequirementsIr`. Each `K:etbCounter` is also registered as a Moved replacement (`card_factory_util::add_etb_counter_replacements`), so the replacement prompt lists it as Java's does; its `ReplaceWith$` is skipped for a Battlefield destination and the counters are still put inline. A Moved-to-battlefield event that carries counters runs in two passes, the Moved replacements and then the AddCounter ones, because Java puts ETB counters as a separate AddCounter event; one combined pass asked a prompt Java never asks (survey g448).
- **A `Defined$ ...LKICopy` reference resolves to the live card's `CardId`; the pre-move object comes from `get_defined_card_objects`.** `DefinedRef::parse` strips the `LKICopy` suffix, and an effect that acts on the card (Pump, PutCounter, ChangeZone, Fight) uses the id, as Java re-reads the card with `getCardState` and checks `equalsWithGameTimestamp` before acting. A reader of state goes through `spell_ability_effect::get_defined_card_objects` with the raw param, which keeps the suffix. CopyPermanent and Clone copy a `Defined$` source through `spell_ability_effect::defined_card_object`, as Java's `getProtoType` and `CloneEffect` copy the object `getDefinedCards` returned (`CopyPermanentEffect.java:247`); copying the live card lost what the move reset, and Niko, Light of Hope's Shards became copies of a bare Naga Fleshcrafter instead of what it had copied. `counters_put_effect.rs`'s `EachFromSource$` branch still tests the param for `LKICopy` and reads `lki::resolve_lki_counters`, whose state-based-death counters come from `last_state_battlefield`, closer to Java than the change-zone table. This is invisible in a short game: the card has to leave the battlefield and the reference has to be read afterwards. A trigger's `TriggeredCardController` reads `Card::lki_controller` when its Card is the LKI (a `ChangesZone` from the battlefield, which also sets `NewCard`), as Java's Card there is the LKI copy; `TriggerController$` (Fake Your Own Death's granted dies trigger) resolves through it and becomes the trigger's controller and decider.
- **`AttackersDeclared` counts `PlayerState::creatures_attacked_this_turn`, not the battlefield.** Java keeps each declared attacker per player (`Player.addCreaturesAttackedThisTurn`, `CombatUtil.java:388`), so an attacker that died or left still counts, and `Count$AttackersDeclared` counts them distinct by (card, zone timestamp) as `equalsWithGameTimestamp` does. The list is cleared in `player_cleanup_turn_state` for the turn player only (`Player.onCleanupPhase`); clearing it at the start of the next turn left the opponent's attackers counted on your turn (Witchstalker Frenzy).
- **Static `Mode$ Always` triggers run when the stack resolves, not only at the next state-based check.** Java runs them inside `checkStaticAbilities` (`GameAction.java:1281`) before `unfreezeStack` runs waiting triggers; `resolve_stack` ends with `run_static_state_triggers`. Do not use `run_waiting_triggers` for a partial pass: it also fires Immediate delayed triggers.
- **Day and night change through `GameState::set_day_time`** (Java `Game.setDayTime`), which checks `CantChangeDayTime` and fires `DayTimeChanges` only between day and night; writing `is_night` directly skips both. The untap step runs `untap::do_day_time` after phasing and then a static pass (`run_static_state_triggers`) before it untaps, as `Untap.executeAt` does; `do_day_time` reads `TurnState::player_previous_turn` and the stack's spells cast last turn. Daybound and Nightbound are keyword traits: two `Mode$ Always | Static$ True` triggers each in `keyword_gen.rs` and Daybound's enters-transformed replacement (`card_factory_util::add_daybound_replacement`), as in `CardFactoryUtil`. Their `CantTransform | ExceptCause$ SpellAbility.Daybound` statics are not generated, because nothing calls `static_ability_cant_transform::cant_transform` yet.
- **A gained ability copy is keyed and resolved through its original ability.** `ActivatedAbility::original_ability` / `SpellAbility::original_ability` are set on `GainsAbilitiesOf`/`GainsAbilitiesOfDefined` copies: the activation table keys on (original ability, grantor static) as Java's `ActivationTable` does, and `ability_utils::get_s_var` reads the original card's SVars before the host's flat `granted_svars` (last source wins there). The harness sorts a gained copy by text because `getPossibleActions` rebuilds the copies before the sort, so two identical gained abilities tie at the first one's index; the parity agent's `ability_declaration_sort_index` mirrors that.
- **CreateToken replacements run once per player over the whole `TokenCreateTable`.** `replace_token_effect.rs` ports the four `Type$` branches (Amount, AddToken, ReplaceToken, ReplaceController) and `ValidToken$` filters by amount; token art RNG is drawn before the replacement, where Java builds the prototype. A declined optional replacement stays in `hasRun` while the event re-runs after another replacement's `Updated` (`run_layer`, `ReplacementHandler.java:233, 248`).
- **This-turn player counters reset at every player's cleanup, not at the active player's turn start.** `PlayerState::on_cleanup_phase` runs for all players from the cleanup step (Java `Game.onCleanupPhase` -> `Player.onCleanupPhase`, `Player.java:2459`): sacrifices, discards, lands, spells, life, crimes, expend and the rest of that list. `new_turn` keeps only what Java resets at the start of a turn.
- **The selector compiler splits a property on every `.`; Java splits only on the first.** A property whose argument is itself dotted (`ControlledBy Player.Active`) must be listed in `lower_compiled_selector`'s nested-argument list, or its argument is cut at the dot and silently never matches.
- **`SelfCard` answers nothing once a trigger's host has moved, while Java's `getDefinedCards("Self")` always returns the host.** An effect that must follow the moved host (an Effect's `RememberObjects$ Self`, Connive's implicit Self) reads `sa.source`.
- **Triggers are laid out base, then pump, then static-granted.** The layer pass keeps the first `base_trigger_count + pump_trigger_count` and rebuilds the rest; `Card::add_pump_trigger` inserts at that boundary, so never push a pump or Animate trigger onto the end. `Card::transform` lifts everything past the face's printed triggers (lasting, pump, static-granted) out before it swaps the face's triggers and puts it back after the new face's printed ones, moving `changed_trigger_count_base` and `base_trigger_count` with it, as Java's changed card traits survive `setState`; swapped with the face, an Animate trigger came back as a printed trigger (Vincent Valentine under Vincent's Limit Break returned from the graveyard forever). That needs an accurate `base_trigger_count`, so `card_assembly` sets it around the back-face Saga transform. Animate type changes are recorded in `Card::changed_card_types` with their timestamp, and a type static re-applies the ones newer than itself (CR 613.7, Java `CardType.getTypeWithChanges`). The battlefield LKI snapshot keeps the type line, as Java's `copyLastState` does.
- **A discard cost records its cards in `SpellAbility::discarded_cost_cards` (Java's `getPaidList("Discarded")`, `CostDiscard.java:222`), never in the host's remembered list.** `Discarded$Valid` and `Defined$ Discarded` read that field, and stack resolution passes the root's list to sub-abilities. Java remembers a discarded card only under `RememberDiscarded` (`Player.java:1441`); remembering cost discards leaked into later `IsRemembered` checks. `finish_activated_ability_on_stack` pays an activated ability's cost with its own `SpellAbility`, so `Discarded$` reads the discard (Mercurial Chemister, Moria Scavenger); a MayPlay alternative cost, foretell's {2} and a mana ability still pay with none. Exile costs (`Exile`, `ExileFromAnyGrave`, `ExileFromSameGrave`) report `Exiled`/`ExiledCards` into `SpellAbility::paid_hash`, as `CostPartWithList.reportPaidCardsTo` does, which is what `Defined$ ExiledCards` reads (Lamplight Phoenix's return). Paid-hash values are numeric ids (`CardId.0`): `CardId`'s display is `Card#N`, which the `Defined$` reader cannot parse.
- **The action space reuses the statics the state-based check applied.** `check_sba` starts each pass with `apply_continuous_effects` and stops on a pass that changes nothing (Java `checkStateEffects` -> `checkStaticAbilities`). When that last pass was not held and nothing was written after it, the check sets `GameState::statics_current_after_sba` and `priority_round` builds the action space without a layer pass of its own. A state-based step that writes the game after the statics pass (the deathtouch-damage clear, the command-zone move, a combat LKI prune) clears the flag, and a new one has to as well.
- **The spell probe shares its mana sources across one action-space build.** `get_playable_cards` builds `SpellProbeSources` once and every hand spell's probe clones its sorted candidates instead of regrouping. That holds only because `group_mana_sources_by_color` reads the payment context through `RestrictValid$` alone, so `SpellProbeSources::new` refuses to share when any source has one; a new context read there needs the same refusal.
- **A batch of state-based deaths holds the layer pass, and damage reads its source's zone-change LKI.** `state_based_actions_pass` sets `GameState::hold_checking_static_abilities` around the destroy, sacrifice and 0-toughness moves, so `apply_continuous_effects` returns early and a dying card's static-granted look-back trigger still sees the others die (Java `holdCheckingStaticAbilities`, `GameAction.java:1626/1656`). A card leaving the battlefield is stored in `GameState::change_zone_lki_info` (Java `Game.changeZoneLKIInfo`), cleared when the stack empties; lifelink and its controller come from it for a source off the battlefield (`DamageDealEffect.java:218`, `GameAction.java:2795`). Deathtouch, infect, wither and the controller that gets the -1/-1 or poison counters come from it too, because Java hands that LKI to `Card.addDamageAfterPrevention` and `Player.addDamageAfterPrevention` as the damage map's row key: `damage_deal_effect.rs`, `damage_all_effect.rs` and `damage_resolve_effect.rs` read the source through `get_change_zone_lki_info`, and so does the deathtouch mark in `GameState::deal_replaced_damage`.
- **A card that changes zones is a new object; nothing about the old one may follow it.** Forge makes a new `Card` and compares objects with `equalsWithGameTimestamp`; this engine keeps one `Card` per id, so every piece of per-object state needs an explicit reset in `GameState::move_card`. What is reset today when a permanent leaves the battlefield: the changed-characteristics baseline (`capture_changed_characteristics_baseline_if_needed` saves type line, power/toughness, keywords and the base trigger count; Earthbend and a `Duration$ Permanent` Animate use it, and `Card::add_lasting_trigger` is the way to grant a trigger for the life of the object; such a trigger is not copiable, CR 707.2, while one an effect writes onto the object itself, `AtEOTTrig$` through Java's `card.addTrigger`, goes through `Card::add_intrinsic_trigger` and is copied, CR 707.9b), the whole until-end-of-turn animate snapshot, `imprinted_cards`, `exiled_cards` (the "exiled with" list, Java `Card.exiledCards`, kept apart from the imprinted list as in Forge), the transformed face, and `cast_from`. `cast_from` and `cast_sa` are set just before the cast moves the card to the stack, before its costs (Forge: `GameAction.changeZone` sets it on the copy before its `checkStaticAbilities`, so a `wasCast` static with `AffectedZone$ Stack` sees the spell), and cleared when the card leaves the stack for anywhere but the battlefield. The stack's cast list keeps each spell's `cast_from` beside its id (`MagicStack::record_spell_cast`), and `card_util::get_this_turn_cast`/`get_last_turn_cast` test a copy with it restored once the live card has moved, as Java's `thisTurnCast` keeps the cast object; the other readers of the cast list still test the live card. `cast_with_flashback` and `cast_with_harmonize` are written on every cast, and the warp mark goes when the card leaves exile. A delayed trigger that must follow one object and not the card sets `DelayedTrigger::target_card_zone_timestamp` (Earthbend's return triggers); it is compared with the card's zone timestamp while it is still on the battlefield and with `Card::lki_zone_timestamp` after the move. `copy_permanent_effect::get_proto_type` builds a copy from that baseline, since keywords, types and power given by effects are not copiable values (CR 707.2). The changed-characteristics baseline is also restored on a move between two other zones (exile to stack), because Java keeps the object only for a move onto the battlefield; a Permanent pump's keyword on a card in exile ends when that card is cast. New per-object state needs the same treatment, or it shows up as a late-game divergence after a bounce.
- **An Effect's `Duration$` goes through `spell_ability_effect::add_until_command` (Java `SpellAbilityEffect.addUntilCommand`) when it ends at a turn boundary.** `UntilYourNextTurn` queues `PhaseCommand::ExileEffect` on `GameState::cleanup` for the controller, and `UntilTheEndOfYourNextTurn` on `GameState::end_of_turn` (`register_until_end` on the controller's own turn, `add_until_end` otherwise), which `cleanup_damage_and_eot` runs as Java's CLEANUP does. The host-leaves durations use the `temp_effect_host` flags, and so do `AsLongAsControl`, `AsLongAsInPlay` and `UntilLoseControlOfHost` for an Effect (their change-of-control and phase-out ends, Java `addChangeControllerCommand` and `addPhaseOutCommand`, are not ported). `UntilEndOfCombat` goes on `GameState::end_of_combat`, `UntilNextEndStep` on `end_of_turn` with `add_at` (no controller, the next end step whoever's turn it is) and `UntilYourNextEndStep` on `end_of_turn` with `add_until` for the controller. Every other duration still ends at the end of the turn: port it into `add_until_command` with the Java `Phase` it names, reading the branch out of `SpellAbilityEffect.addUntilCommand`. Note that a probe rarely observes a duration fix — the effect has to outlive the turn it was made in and then matter — so audit the parse and the phase hook rather than expecting card movement. A Pump's keywords with either next-turn duration go there too, as `PhaseCommand::RemoveKeyword`, and with `AsLongAsControl` or `UntilLoseControlOfHost` they end when the host leaves, as with `AsLongAsInPlay`. Its power and toughness with any duration past this turn are a timestamped `Card::pt_boosts` entry that `PhaseCommand::RemovePtBoost` removes (`PumpEffect.java:94-122`), and `PumpAll` shares that path. A goad lasts `UntilYourNextTurn` unless the Goad names another `Duration$`, and ends through `PhaseCommand::RemoveGoad`.
- **An `Animate` reverts on the schedule its `Duration$` names, and the cleanup step only undoes the end-of-turn ones.** `Animate` and `AnimateAll` both call `add_until_command` with `PhaseCommand::RestoreAnimate` first and record its answer: `AnimateState::ends_at_end_of_turn` is true when nothing was registered (no `Duration$`, or a duration `add_until_command` does not model), and the CLEANUP sweep in `phase_handler.rs` restores only those, so an unmodelled duration still ends at end of turn as Java's final `else` does. Java registers the revert once, in the shared `AnimateEffectBase.doAnimate`, so the two Rust effects must stay in step. The snapshot belongs to the face it was taken on: `Card::transform` puts the old face back from it, re-takes it on the new face, and carries the Animate-set power, toughness and color and the `changed_card_types` across, as Java keeps those on the `Card` (`newPT`, changed types and colors) and not on the `CardState`. Animate records what it set on the `AnimateState` (`add_new_pt`, `add_color`, Java `addNewPT` and `addColor`), and `apply_new_pt_and_color` puts it on the new face, and on a clone of the animated card; comparing the card with the snapshot misses an Animate that sets the printed value; restoring the old face's snapshot onto the new one made Galian Beast a 2/2 Assassin. Do NOT move that `else` into `add_until_command` itself: its `false` is load-bearing, because callers own some durations through other mechanisms (an Effect's `temp_effect_host` flags). A blanket end-of-turn fallback there was tried and reverted: 10 probe cards went PASS -> DIVERGES and 3 survey, 114 meta and 87 round-robin matchups regressed. Before this the sweep restored EVERY battlefield card's snapshot unconditionally, so a `Duration$ UntilYourNextTurn` animate created during its own controller's end step was undone the same turn. `clear_damage()` is keyed off whether the card had an animate state at all, NOT off whether the restore ran — moving it inside the restore branch regresses any card whose animate outlives the turn (measured: Gastal Thrillroller PASS -> DIVERGES on `battlefield[i].damage`). `AnimateEffectBase:62-76` builds a `Set<RemoveType>` from seven `Remove*Types` params; `RemoveCardTypes`, `RemoveSuperTypes`, `RemoveSubTypes` and `RemoveCreatureTypes` are ported in `animate_effect.rs`, the other three are not, and `animate_all_effect.rs` still reads `RemoveCreatureTypes` only. Any of them must be followed by `staticability::layer::sanitize_subtypes`, as Java recomputes subtypes whenever the type line changes (`CardType.sanisfySubtypes`) — dropping Creature has to drop the creature subtypes with it.
- **`BecomesTarget` fires when the spell or ability reaches the stack, for every target in the chain.** `emit_becomes_target_triggers` runs from `emit_post_stack_spell_ability_triggers` for spells and activated abilities alike, walks the sub-abilities (a Charm keeps its targets on the chosen mode) and fires `BecomesTargetOnce` once. Do not fire it when targets are chosen: a cast cancelled at its cost must not trigger anything.
- **Two agent callbacks mirror `PlayerController` methods whose human version answers without a prompt.** `order_move_to_zone_list` carries the destination zone (the interactive agent returns the cards unchanged for Hand and Graveyard, as `PlayerControllerHuman` does with default preferences), and `choose_cards_to_discard_unless_type` defaults to `choose_discard`. `GameNotification::SpellPaymentFailed` is sent from every cast rollback from the cost adjustment on, and not for a Charm choice, an announcement or targeting; the parity harness uses it to drop the card from the action space for the turn, as `HarnessPlayPlumbing` does.
- **Castability checks mirror `ComputerUtilMana`, quirks included.** A `RestrictValid$` mana source is tested against the spell being paid for only. For a Secondary-face cast (Adventure, Omen) the probe tests `Spell.<Type>` against the card as it sits in hand, because Forge asks `sa.getHostCard().hasProperty`; the payment, with the card on the stack, uses the face being cast. A mana ability whose `PayLife` would leave the player below 1 is not counted ("don't kill yourself"). A `Waterbend` cost counts mana only, not the creatures and artifacts it could tap, because `calculateManaCost` clears `maxWaterbend` in test mode when the payer's controller is not the AI (`cost_waterbend::can_pay`). A Waterbend arriving through `Mode$ RaiseCost` has to be added to the required mana by hand (`playability::raise_mana_from_cost`): `mana_from_cost` sums only `CostPart::Mana`, and `can_pay_ignoring_mana_for_spell` answers true for a Waterbend, so a raise cost carrying one would otherwise be free on both paths. The cast merges that Waterbend into the spell's one mana payment in `cost_adjustment::adjust`, as `Cost.add` merges a `CostPartMana`, and `cast_spell` strips it from the raise cost it pays afterwards. Convoke and Improvise are applied before any mana source, each untapped creature or artifact in name order paying one shard of its colour (`computer_util_mana::adjust_cost_by_convoke_or_improvise`, the harness's probing `chooseCardsForConvokeOrImprovise`); the payment itself convokes after the mana sources. A cancelled cast leaves the artifacts tapped for Improvise tapped: Java's `rollbackAbility` untaps only `getTappedForConvoke()`, and Improvise never adds to that list. Mana from `TapsForMana` triggers (Mana Flare, Lavaleaper) is predicted by `add_taps_for_mana_trigger_mana_impl` in both the probe and the auto-pay, as `ComputerUtilMana.predictMana` does: only triggers that could run (`getActiveTrigger`: zone, `IsPresent$`, limit, mode test), a `Mana` trigger adding its `Produced$`, a `ManaReflected` trigger one of the mana the land made. The graveyard and exile cast checks go through `available_mana_for_spell_card` so that they apply the same restrictions as the hand check. The face-down cast runs the same probe on the face-down copy with `is_cast_face_down` set, so `RestrictValid$ Spell.isCastFaceDown` mana (Tin Street Gossip, Qarsi Deceiver) counts toward it; the cast takes that spell's raise-cost parts from the face-down copy too (`cast_spell`), so a `ForEachShard$` raise (Drought) counts no pips, and the hand check offers the face-down option whatever the face-up cast's additional costs allow. `GameState::action_space_mana_probe` picks the spell probe: `AutoPay` (the default, and the app's) simulates the engine's own payment; `ComputerUtilMana` (the parity default) ports `ComputerUtilMana.canPayManaCost` in test mode (Java's source order, `sortManaAbilities`, `chooseManaAbility`, `payMultipleMana`), which refuses some payable costs as Forge does, and it turns off the source-level fallback in `playability.rs`. An unless cost has its own quirk: `DeterministicController.payCostToPreventEffect` skips a payer only when `ComputerUtilCost.canPayCost` and the harness's `canPayManaCostFromCurrentSources` both refuse, and the latter ignores the X shard, so `UnlessCost$ X` is always attempted — AutoPay taps what it can, cancels, and the taps stay. `resolve_effect_with_unless_cost` mirrors that with `EffectCostPaymentMode::Unless { attempt_unpayable }`: no pre-check, and a cancelled auto-payment keeps its taps through the `AttemptedAndFailed` trace the cast path already uses. `pay_ability_cost` does the same for a triggered ability, which Java never rolls back: when its auto-payment fails, the rollback re-taps the sources AutoPay tapped and puts back the pool they filled. A failed activated ability is rolled back as Java's `rollbackAbility` does instead: the lands untap and the host keeps the `{T}` it paid first. The harness asks `pay_mana_cost` only for a cost that is not zero, so `pay_mana_cost_for_effect` skips a zero cost, but an `UnlessCost$ X` on a spell that announced no X is a bare X shard in Java, not zero: `calculate_unless_cost` keeps the shard, and the payer is asked and pays nothing.
- **Auto-pay spends floating mana the way the harness `AutoPay` does, and asks `choose_mana_from_pool` on a tie.** After each source resolves, `ManaPool::pay_mana_from_ability` pays with that source's own mana, then `pay_mana_cost_from_pool` walks the unpaid shards in `ManaCostShard` order and takes each one's mana from `CostPayment::get_mana` (Java `selectManaToPayFor`: +5 colorless, or a new colour for converge, +2 restricted, +1 non-snow); distinct best-weight mana go to the agent. The options are listed in Forge's pool order: colours in the order they entered the pool, and a colour that empties is replaced by the last one (Guava's `ArrayListMultimap` over a `CompactHashMap`), which `ManaPool::floating_mana_keys` tracks. Only the spell and effect paths (`consume_incrementally`) pay this way; activated abilities still tap first and pay the whole cost from the pool, so they never ask. A source's own mana goes into the cost through `ManaCostBeingPaid::is_needed` and `pay_mana`, which take the `Mana` itself because only snow mana pays `{S}` (Java `canBePaidWith`); the color-only `try_pay_mana` can never pay `{S}`, and spending a snow land's `{C}` that way put it on a generic shard and left `{S}` unpaid (Mouth of Ronom).
- **Every cost part decides its cards before any part is paid.** Java's `CostPayment.payComputerCosts` runs each part's decision and only then pays the parts in order, so a typed exile, an exile from any graveyard, a Return, a tapXType, Collect evidence, Reveal, Blight and Forage are chosen before `pay_mana_cost` is asked. `pay_ability_cost` phase 1 fills a decision table indexed like `cost.parts` (`decides_cost_part_cards`, `decide_cost_part_cards`) and phase 2 pays from it; the cast path pre-chooses the same parts for `spell_cost` and `raise_cost` (`prechoose_additional_cost_cards`). A decision with too few candidates fails the payment before any mana is spent, as Java's null decision does. Still decided while paying: ExileFromSameGrave, Exert, GainControl, Unattach, UntapType, PutCardToLib, ExiledMoveToGrave, ExileFromStack, Enlist and AddCounter; one of those next to a decided part asks the agent in a different order from Java, as Lamplight Phoenix's own exile did against its Collect evidence. `GameState::mirror_forge_bugs` is the switch for reproducing a Forge rules bug: the parity runner turns it on, and the engine default keeps the rules. Under it, a decided Collect evidence pays as `CostCollectEvidence.doListPayment` does: no recheck of the total, and a card an earlier part already exiled (Lamplight Phoenix's own "exile it") counts but drops out of `ExiledCards`, because Forge re-exiles that object and the earlier paid list no longer finds it.
- **An activated ability's mana cost is adjusted like Java's `CostAdjustment.adjust`.** `cost_adjustment::adjust_ability_mana_cost` applies the ability's `ReduceCost$`/`ReduceAmount$`, PowerUp (the host's mana cost, if it entered this turn) and the `ReduceCost`/`RaiseCost` statics whose `Type$ Ability` or `ValidSpell$ Activated.<property>` accepts the ability, in both the activation check and the payment (`GameLoop::adjusted_activation_cost`). Plot, a Room's unlock and the turn-face-up ability are `AB$` abilities here but `AbilityStatic` in Java, which is not an activated ability: `ActivatedAbility::is_ability_static` names them, `Type$ Ability` and `Activated.` skip them and only `ValidSpell$ Static.<property>` matches them (Doc Aurlock's `Static.Plotting`, Inquisitive Glimmer's `Static.Unlock`). They resolve through `resolve_immediate_ability` and the unlock is offered from `get_playable_cards`, not `get_activatable_abilities`, so both of those adjust the cost as well; paying the printed cost made Rust offer a reduced plot and then cancel it. A mana part that comes to zero is not paid, so no `pay_mana_cost` callback runs, as in the harness.
- **A `MayPlay$` grant with `MayPlayAltManaCost$` adds a copy of each hand option that pays that cost instead of the mana cost** (Java `GameActionUtil.getMayPlaySpellOptions`): `PlayCardMode::MayPlay(base)`, with `base` the option it copies (`None` for the spell, `Some(Morph)` for the face-down cast) and `alt_cost_index` the grant in `static_ability_continuous::may_play_alt_costs` order. The cost is parsed as a full `Cost` (`CollectEvidence<10>`, `PayLife<...>`) and paid through the static-alternative path. A grant made with `MayPlayDontGrantZonePermissions$` casts from hand only; the graveyard and exile loops ask `grants_zone_permissions`. The exile loop, like the library loop, offers one option per grant, as Java builds one per `CardPlayOption`: `Normal` at the printed cost for a grant without an alternative cost, `MayPlay(None)` at the grant's own cost for one with it (a Room's right half too), and `may_play_spell_grant_source` records the grant the option came from so its `MayPlayLimit$` is spent. The parity agent orders the copies by Java's `" by <host> (<id>)"` label, taking the Java id as the Rust id + 1, which drifts for a token or effect host. The graveyard, exile and library loops number their `Normal` copies by grant (`alt_cost_index` is the grant's position among grants without an alternative cost), and the agent's label carries the grant's `MayPlayText$`, as Java's `" by <host> (<MayPlayText>)"` does.
- **A `Play$` effect plays a land as a land.** `get_spells_from_play_effect` offers a land face as a land play (`SpellAbility::new_land`) only on the controller's turn with a land play left, as `AbilityUtils.getSpellsFromPlayEffect` checks `canPlayLand` ignoring zone and timing, and `play_effect.rs` puts it onto the battlefield and counts the land play instead of casting it (`LandAbility.resolve`).
- **A keyword granted by a static or a pump does not bring its traits by itself; the layer pass rebuilds them.** `keyword_gen.rs` turns `K:Ward:`, `K:Plot:`, `K:Embalm:` and the rest into triggers and abilities at card construction, and nothing re-runs it for an `AddKeyword$` or a `KW$`. Java rebuilds every keyword's traits whenever the keywords change (`addChangedCardKeywords` -> `createTraits`). Here the layer pass truncates `activated_abilities` to `base_ability_count` and `triggers` to `base_trigger_count` on every pass and then rebuilds: `generate_keyword_activated_abilities(&keywords)` takes the list it builds for, so the pass hands it each card's `pump_keywords` + `granted_keywords` after the effects loop (Cursecloth Wrappings' `KW$ Embalm:CardManaCost` on a graveyard card), and a statically granted keyword that is not redundant with an intrinsic one gets its triggers from `generate_keyword_triggers_for`, as a pumped keyword does (Java `getKeywordForStaticAbility` -> `createTraits`, redundant instances dropped as `KeywordInstance` does); the generator's SVars go into `granted_svars`, which each layer pass clears. Do not build a keyword's trigger by hand in the `GrantKeyword` arm: that is how a granted Miracle once triggered twice. A keyword side effect that is not a trait (Modular's enters-with counters, `add_keyword_etb_counters`) stays out of the trigger generators, or every layer pass would add it again. A cost of `CardManaCost` resolves to the card's own mana cost inside the generator. `replacement_effects` has no such truncation, so a granted keyword's replacement (Riot, through `card_factory_util::riot_replacement`) goes in as a `-2` changed-traits layer, which `clear_static_layer_changed_card_traits` drops at the top of the next pass like the `-1` layer of granted statics; the replacement carries its `ReplaceWith$` ability as a trait SVar because the host never had the keyword's SVar. An ETB replacement granted this way is only reachable through CR 614.12: the entering card is still on the stack, where no static grants it anything, so `ReplacementHandler::run` builds `battlefield_pre_list` (Java `getReplacementList`'s `affectedLKI` + `checkStaticAbilities(preList)`) — a clone of the game with the card on the battlefield, one `apply_continuous_effects`, and that card stands in for the real one in `collect_effects` for the whole event. It runs only when some static in a static-source zone grants Riot; a new keyword trait that matters on entry has to be added to both the `GrantKeyword` arm and that gate.
- **A static ability works from any zone in `ZoneType.STATIC_ABILITIES_SOURCE_ZONES`** — Battlefield, Graveyard, Exile, Command and Stack (`ZoneType.java:37`), not just the battlefield. `StaticAbility::check_conditions` already gates each static on its host's zone and defaults to Battlefield, so a collector that widens its search is filtered back down by `EffectZone$`/`ActiveZones$` and nothing else changes. A collector that walks fewer zones instead makes a whole class of card silently inert: `may_play_grants` walked Battlefield and Command only, so every `EffectZone$ Graveyard` self-grant ("You may cast this from your graveyard") was never found and those cards could not be cast from the graveyard at all. When a static reads correctly and still does nothing, check the zone list of whatever collects it. `ZoneType::is_static_ability_source` is the shared spelling of the set and `Card::clone_for_parity_snapshot`/`refresh_parity_snapshot` use it too, so a card outside those zones carries no statics into the harness agent's view; a new collector calls the helper rather than writing its own match. `attack_cost.rs`, `block_cost.rs` and both DisableTriggers collectors (`static_ability_disable_triggers::is_disabled`, `TriggerHandler::is_trigger_disabled_by_static`) walk the set and gate with `check_conditions_full`; walking the battlefield alone left an Effect's `CantAttackUnless` in the Command zone (Summon: Yojimbo) inert. A DisableTriggers source that left the battlefield in the current SBA batch is still read, without its conditions, as Java reads its battlefield LKI copy. `cost_adjustment.rs` is the exception: Java's `CostAdjustment.adjust` collects its statics from Battlefield, Stack and Command plus the spell's own card, and both loops (the mana adjustment and the `Cost$` parts of a RaiseCost) walk that list and share one `check_requirement`, which calls `check_conditions` and tests `AffectedZone$` against the zone a spell was cast from, or the host's own zone for an ability or a spell not yet cast; without it Agatha of the Vile Cauldron's battlefield-only reduction reached the cycling of creature cards in hand. The `Cost$` loop used to walk the battlefield and read `EffectZone$` against the cast zone, so an emblem's `Cost$ B` never applied.
- **A spell's target gate is its cast ability chain, as in `ActionSpace.hasValidTargets`.** Every zone path, the hand included, asks `has_candidates_in_spell_ability_chain` on the ability it would cast, which counts battlefield and stack candidates alike. A second check built from the spell's text looked only at the battlefield for a multi-clause `ValidTgts$` and kept Sudden Setback from ever targeting a spell.
- **A granted keyword can reach a card's other face only through the alternate host.** Java offers an Adventure or Omen half from the graveyard with a granted Flashback because `GameActionUtil.getAlternativeCosts` reruns `checkStaticAbilities` on `Spell.getAlternateHost`, an LKI copy in the Secondary state (CR 601.3e). `spell::alternate_host_with_statics` does the same on a clone of the game (only when a static in play grants Flashback and the card has a Secondary face), `playability.rs` offers those costs after the card's own (`flashback_costs`), and `cast_spell.rs` adds the flashback exile replacement to the stack card when the cast face has none. Flashback's exit from the stack goes through `apply_moved_replacement`, as Harmonize's does, not a hard-coded Exile.
- **`playability.rs` walks the zones `ActionSpace.getPossibleActions` walks, and the library is one of them.** The harness collects hand, battlefield, graveyard, exile, command and then `lib.get(0)` for **every** player, and lets `getAllPossibleAbilities` filter; the Rust loop adds the top card of every player's library and lets `can_may_play_from_static` filter. Leaving a zone out is invisible from the card's side: a grant with `AffectedZone$ Library` reads correctly, its filter compiles to a real predicate, and it still offers nothing, because no candidate ever reaches it. `ContextPredicate::TopLibrary` was hard-coded `false` at the same time, so each defect hid the other and fixing either alone measured zero cards. When a play-from-zone grant measures nothing, check the zone list before the filter. `TopLibrary` compares against the **last** Rust element of the owner's library (Java `CardProperty:610` takes `get(0)`, and the two orders are reversed); `TopLibrary_<filter>` and `BottomLibrary` are unported.
- **One card's simultaneous triggers follow Forge's trigger ids, and those depend on how the card object was built.** The parity harness orders them by host timestamp and then trigger id. On every zone change `CardCopyService.copyCard` rebuilds a card that has a paper card from its script: `T:` triggers first, keyword triggers after. A keyword trigger (Evoke's sacrifice) therefore has the higher id, is pushed after the card's own enters triggers and resolves first. A token or a copied permanent goes through `copyStats` (`CardState.copyFrom`), which copies the keywords first. `register_evoke_sacrifice_triggers` sets `DelayedTrigger::sort_after_active` from that rule; a new keyword trigger modelled as a delayed trigger needs the same decision. A player's effect cards (speed, the monarch, the initiative, City's Blessing, radiation, An Enduring Story) enter the command zone without a timestamp (`add_player_effect_card`), as Forge's `PlayerZone.add` gives them none, so their triggers sort before that player's other simultaneous triggers.
- **Auto in an interactive payment floats mana, as the Forge host's `AutoPay.floatManaForCost` does.** `PlayerAgent::auto_pay_floats_mana` is true only for `PromptAgent` (app seats and the SimpleAi bot). For those seats `Pay { auto: true }` runs `float_mana_for_cost` in `game_loop/mana_payment.rs`: it picks sources in auto-pay order (`computer_util_mana::next_auto_float_choice`) and activates each through the manual-tap path, so every floated source gets its own mana-undo record. It stops at the first declined source cost or when no candidate is left, pays if the pool then covers the cost, and otherwise returns to the prompt with the mana floating. A Cancel from these seats restores the whole cast, as the host's CANCEL (`restoreManaPaymentSnapshot`) does, whether the mana came from Auto or from manual taps: the lands untap and the floated mana leaves the pool. Every other agent keeps the `AutoPay.payManaCostWithTrace` port: a declined source is asked again until the 128-step guard runs out, an unfinished Auto fails the payment, and a Cancel returns `failed_preserving_taps`, so the lands stay tapped and their mana is gone.
- **A mana ability's own cost is paid inside `computer_util_mana` during auto-pay, which has no trigger handler.** Java pays it with the same `CostPayment.payComputerCosts` as any other ability, so a cost move that fires triggers goes back to the game loop through a `ManaPayCallback`: `NotifySacrificeForMana` (`perform_sacrifice`) and `ExileCostCardsForMana` (`exile_cost_cards`, shared with `pay_collect_evidence_cost`: ChangesZone for each card, then `CollectEvidence`, then ChangesZoneAll, as `CostCollectEvidence.doListPayment` and `CostPartWithList.handleChangeZoneTrigger` do). A callback without the runtime returns `None` and the payer moves the cards itself. Without it, Cryptex's collect evidence fires neither Surveillance Monitor nor a card-leaves-your-graveyard trigger.
- **A mana payment session reaches the game, the agents and the pools only through its `ManaPaymentHost`, and never holds them across a host call.** `pay_mana_cost_session_generic` takes them from `parts()` again after each call, because a call can replace the pools: a mana ability whose cost fails (a declined Treasure sacrifice) rolls back through `restore_snapshot`, which puts a new `Vec` in `GameLoop::mana_pools`. The session used to hold `&mut self.mana_pools` while its closures reached `GameLoop` through a raw pointer, so that rollback freed the pools under it and an interactive payment crashed or read garbage. A `ManaPayCallback` that moves cards or runs replacements carries the `&mut GameState` it acts on for the same reason. Do not give a payment helper a raw pointer to state its caller has borrowed.
- **An ability that runs inside a Moved replacement gets the game runtime.** `move_card_internal` takes the `ReplacementRuntime`, so a `ReplaceWith$` chain on a permanent entering the battlefield (an enters-as-a-copy Clone, its `ImmediateTrigger`) registers delayed triggers on the real `TriggerHandler` and draws from the game RNG. State-based deaths get it through `SbaReplacementParts` (`GameLoop::sba_runtime`), so Head of the Hunt's `ImmediateTrigger` makes its Wolf. `GameState::move_card` and `move_card_with_agents` have no runtime and still fall back to a local handler; move through `EffectContext::move_card` or `GameLoop::move_card_with_runtime` wherever one is in reach. A discard gets one too (`GameState::discard_card` takes the runtime; effects call `EffectContext::discard_card`, the loop passes `replacement_runtime()`): without it Progenitus's `ReplaceWith$` shuffle drew from `ThreadRngAdapter`, invisible to the RNG count and different on every run. So does a sacrifice: `perform_sacrifice` (Java `GameAction.sacrifice`) takes the runtime, cost payments pass `replacement_runtime()` and the Saga sacrifice builds one from `SbaReplacementParts`; without it Kalitas, Traitor of Ghet's replacement for a creature sacrificed as a cost ran with no token templates and panicked. A permanent sacrificed to pay for mana goes through `perform_sacrifice` with a runtime too: `make_mana_payment_callback` takes one from `GameLoop::mana_payment_runtime` (the effect-side payers in `ability/effects/cost_payment.rs` and `roll_dice_effect.rs` build their own from the `EffectContext`), whose `mana_pools` is a fresh set because the payment holds the payer's pool. `ReplaceWith$` always names an SVar, even `ReplaceWith$ Exile`; only the engine's own `NewDestination$` redirects by zone name. A result other than `NotReplaced` or `Updated` ends the move, as in `GameAction.changeZone`, because the replacement's ability already moved the card; a new Moved call site must check the result too. A move with a trigger handler also registers the card's triggers in its new zone, as `GameAction.changeZone` does; after a move without one, an effect card's `Static$` trigger stays inactive until the next `reset_active_triggers`.
- **A zone's card list runs oldest first in Rust and, for the moves the probes see, newest first in Forge, but a hidden-origin ChangeZone sorts its fetch list by `Card.toString()` before it picks.** `moveTo(ZoneType, …)` inserts at index 0 and a library's top is its last Rust element. `ChangeZoneEffect.changeHiddenOriginResolve` calls `fetchList.sort()` (`Ordering.usingToString()`, `"Name (id)"`) whatever the zone order, so an `AtRandom$` pick draws one `nextInt(size)` per card over that order (`change_zone_effect::search::resolve_random_selection`). Cards with the same name tie-break on the card id, and the port's ids are not Forge's.
- **A `HIDDEN ` keyword keeps its prefix in the keyword sets, and `has_keyword` matches it without the prefix** (Forge: `addHiddenExtrinsicKeywords` stores `kw.substring(7)`, `Card.hasKeyword` checks the hidden list). The parity snapshot leaves `HIDDEN` entries out, as Forge's keyword list does. A `Duration$ Permanent` Pump puts its keywords in the changed-keyword set, which cleanup does not clear; the untap step removes `This card doesn't untap during your next untap step.` from the active player's permanents after untapping, as `Untap.doUntap` does.
- **`SpellAbility::description` is empty for abilities built from a script.** Java's `AbilityFactory` sets it from `SpellDescription$`; the port does not, and `ir.sp_desc_text` reads `SpDesc$`, which no script has, so `rebuilt_description` falls back to the ability text. Where Forge reads a sub-ability's `getDescription()` as data, read `ir.spell_description_text`, as `choose_generic_effect.rs` does for `SetChosenMode$` (the `ChosenMode<X>` card property).
- **`Execute$` abilities and sub-abilities are looked up by name on the host when they resolve; Forge builds them with the ability.** The exception is the `Execute$` of a `DelayedTrigger` or `ImmediateTrigger`: `build_spell_ability_for_host` copies that SVar onto `SpellAbility::svars` when it builds the ability, as `AbilityFactory.getAbility` calls `setSVar` for those two APIs, and `ability_utils::get_s_var` reads the ability's own SVars first. Without that, a keyword granted by a counter or a static (a decayed counter from Rot-Curse Rakshasa) lost its `granted_svars` when the creature died with the trigger on the stack, and the delayed trigger was registered with the bare SVar name and panicked when it fired. Anything that swaps a host's SVars in the middle of its own chain has to keep the host's SVars as a fallback, as `clone_effect.rs` does when a card clones onto itself. A copy that leaves the battlefield keeps its copied SVars behind the restored ones (`Card::restore_clone_snapshot_keeping_svars`), so a trigger it already put on the stack still finds its sub-abilities. `ImmediateTrigger` and `DelayedTrigger` remember `RememberObjects$` on the trigger, resolved through `get_defined_entities`, never on the host. A trigger granted to another card (Animate's `Triggers$`) therefore has to be given the whole chain, not just the `Execute$` SVar: `animate_effect::copy_execute_chain_svars` follows each `SubAbility$` from the source's SVars onto the target, because Forge attaches the built sub-abilities to the trigger and this port looks them up by name at resolution. Copying only the first name resolved the trigger and silently dropped everything after it — Fake Your Own Death returned the creature and never made its Treasure.
- **A clone or face-down state carries its own changed-traits baseline.** `Card::trait_base_*` stand for the current state's own traits, the list a changed-traits layer (a static's granted keyword or ability, Animate) is applied over; Java keeps them on each `CardState`, so `Card.removeCloneState` switching `currentState` back switches the base with it. `clone_effect.rs` rebases onto the copy (`reset_changed_card_traits_baseline_to_current`), `set_original_state_as_face_down` drops it so the changes apply over the empty face-down lists (Java `setState(FaceDown)`), and `CloneState` saves the original baseline for `apply_clone_state` (unclone and `turn_face_up`) to put back. A trait added to the face-down state after it is built (the cloak and Disguise `Ward:2`, the turn-face-up ability) goes through `add_intrinsic_keyword_with_triggers` or `Card::add_intrinsic_activated_ability`, which write it into the baseline as well, as `add_intrinsic_trigger` does. Without that, the layer pass's `clear_static_layer_changed_card_traits` restored Fear of Impostors' trigger onto a Floodpits Drowner after its Fleeting Reflection copy ended, next to Drowner's own SVars, and building the trigger panicked on the missing `Execute$`. A copy of a face-down permanent takes the face-down characteristics (Java `getCloneStates` copies `getFaceDownState()`): `clone_effect.rs` clears the kept `card_name` and drops the turn-face-up abilities, which this port puts on the face-down card where Java keeps them on the original state behind `IsPresent$ Card.Self+faceDown`.
- **A face-down permanent keeps whatever `static_set_power`/`static_set_toughness` it had.** The layer reset in `staticability/layer.rs` skips them for face-down cards so a morph's 2/2 override survives, so a path that turns a permanent face down on the battlefield clears them (`card_factory_util::turn_face_down_with_state`, which `Mode$ TurnFaceDown` uses), or Ixidron's own `*/*` value stays on it after another Ixidron turns it face down.
- **`Defined$ Valid <filter>` resolves in `ability_utils::get_defined_valid_cards`** (the `Valid` branch of Java `AbilityUtils.getDefinedCards`): `Valid ` searches every battlefield, `ValidAll ` every zone, and `Valid<Zone> ` that zone for every player, filtered with the ability's context. `get_defined_cards` and the ability-aware `resolve_defined_cards_for_sa` both go through it; an effect that parses `Valid` itself (`clone_effect.rs`, `airbend_effect.rs`) predates it.
- **An effect that acts on players and cards picks them with `spell_ability_effect::get_target_entities`** (Java `getTargetEntities`): the targets when the ability targets, otherwise the `Defined$` cards and players. Only player-capable refs (`You`, `Targeted`, `TriggeredTarget`, `Remembered`, ...) reach `resolve_defined_players_with_sa`, because that resolver answers any value it does not know with the activator; asking it about `Self` is how `DealDamage | Defined$ Self` used to damage its controller. DealDamage also takes its sources from `DamageSource$` and stores excess damage to cards in `ExcessSVar$` on the host.
- **A triggered ability pays its `Cost$` before the effect resolves, with no exceptions.** `WrappedAbility.resolve` ends in `playSpellAbilityNoStack`, and both controllers reach `ComputerUtil.playNoStack`, which is `pay.payComputerCosts(...)` and only then `AbilityUtils.resolve(sa)`. Forge has no path that resolves a triggered ability's effect first. This matters most where the cost reads the same zone the effect changes: a `T:` whose `Execute$` is `AB$ Draw | Cost$ Discard<1/Card>` must choose the discard from the hand as it stands BEFORE the draw, or the candidate list is bigger than Java's and the two agents pick different cards from the same RNG draw.
- **A trigger's condition is checked twice: when it fires and again when it resolves.** `stack_resolution::trigger_requirements_still_met` (Java `WrappedAbility.resolve`) re-runs `Trigger::requirements_check` before an `is_trigger` entry resolves and does nothing when it now fails, skipping that only for an `Always` trigger or one with `NoResolvingCheck$`. It then runs `Trigger::meets_requirements_on_triggered_objects` (Evolve, Increment, `Condition$`) on a `RunParams` rebuilt from the ability's triggering objects, as Java passes `getTriggeringObjects()`: two Beasts entering together (The Huntsman's Redemption under Doubling Season) gave Sharp-Eyed Rookie two counters when the second trigger should have found it no longer smaller. The cast ability that Increment, `LifePaid` and `Sacrificed` read is passed by reference: `RunParams::spell_ability` when the trigger fires, and the triggering `AbilityKey::SpellAbility` that `RunParams::add_common_trigger_objects` stored on the ability when it resolves.
- **`GameLoop::run_static_state_triggers` re-tests each matched trigger just before it runs it.** It matches every static `Always` trigger at once and resolves each through `resolve_stack`, whose end runs `run_static_state_triggers` again, where Java's `runStateTrigger` tests `canRunTrigger` for each trigger as it reaches it. Without `TriggerHandler::can_run_state_trigger`, a Nightbound transform matched in the outer pass ran after the nested pass had already transformed the card, and flipped it back.
- **A later sub-ability may not repeat an earlier one's target when it has `TargetUnique$`.** Java reads the ancestors' targets back up the parent chain in `getUniqueTargets`; there is no parent link here, so `SpellAbility::setup_targets` and `setup_new_targets` collect them going down and write `SpellAbility::unique_targets` on each node before it picks. They also write each sub-ability's `parent_targeting_card` and `parent_targeting_player` from the nearest node above that targeted one, as Java's `setParent` is there for (`SpellAbility.java:2149`, `:2183`), so a sub-ability's `TargetsWithDefinedController$ ParentTarget` names the player the node above targeted (Rite of Renewal); without it the list was empty and every graveyard's cards were candidates. The flags that compare a candidate against the same node's already-chosen targets (`TargetsForEachPlayer`, `TargetsWithSameController`, `TargetsWithoutSameCreatureType` and the rest) go through `SpellAbility::relational_target_ok`, `TargetsWithSameCardType` among them (`TargetRestrictions.java:181`; it was read from `TargetsWithSharedCardType`, so Burglar's Plot could exchange an artifact for a creature). With any of them set, `choose_targets_by_kind` picks one target at a time through `PlayerAgent::choose_next_target_card` and re-filters the candidates with `relational_target_ok` after each pick, as Java's `TargetSelection` does, so every agent is held to them; the default asks `choose_target_cards` for one card, and the parity agent's override draws Java's `pick_one` per pick and the single stop-or-continue `pick_bool`. Without them a node's targets are still picked in one `choose_target_cards` call. Only the parity agent used to re-filter, and SimpleAi returned Behold the Sinister Six! targets with the same name. The `hasParam` checks that do not compare against the node's own targets (`TargetsWithDefinedController`, and `TargetsWithSharedCardType` with `TargetsWithSharedTypes`, which compares against a `Defined` card) go through `SpellAbility::param_target_ok`, from `can_target` and every candidate list in `setup_targets`. `TargetsWithControllerProperty` and `TargetsWithRelatedProperty` (against the first card an ancestor targeted) are there too; `TargetsAtRandom` is still unported.
- **A `SpellCast` trigger with `TargetsValid$` only sees spells that target a matching entity.** The test walks the cast spell's sub-ability chain and passes on the first targeted entity that matches (Java `TriggerSpellAbilityCastOrCopy.performTest`); a spell that targets nothing never satisfies it. `IsSingleTarget$` needs exactly one distinct target across the chain, so a targetless trigger never satisfies it either.
- **A triggering spell or ability is shared, not copied.** `SpellAbility::trigger_spell_abilities` holds `Arc<SpellAbility>`, as Forge's triggering objects are references, so a trigger that fires on a trigger does not copy the chain beneath it; the setters that recurse into the map (`set_host_card_id`, `apply_text_change` and the rest) go through `Arc::make_mut`, since the map also stands in for Forge's `additionalAbilities`, which Forge copies. The trigger push in `TriggerHandler::process_pending_triggers` gives `SpellAbilityCast` only `spell_ability`, as `MagicStack.add` passes only `SpellAbility`. `zone::magic_stack::stop_infinite_loop` is the head of `MagicStack.add`: once the stack passes 999 entries the game ends in a draw and nothing more is pushed. Every push calls it: triggers, casts and activations, copies, Replicate, CastFrom and Play.
- **An effect acts on every card its ability targets.** `get_target_cards` and `Defined$ Targeted` return `TargetChoices::all_target_cards`, the first target and the rest (`divided_map`), as Java's `getTargetCards` does; reading `target_card` alone taps, pumps or destroys only the first of several targets.
- **A sub-ability that targets keeps only its own targets.** `Defined$ Targeted` and `ParentTarget` on a later sub-ability read the nearest ancestor that targeted something (Forge's `getParentTargetingCard`/`getParentTargetingPlayer`), so the three chain walkers (`stack_resolution.rs`, `effect_resolver.rs`, `replace_moved.rs`) carry the last non-empty target forward and hand it only to a sub-ability without `ValidTgts$`. Together as One's `DealDamage` used to hit both its own target and the player its `Draw` targeted. A sub-ability that inherits the target card inherits the parent's other target cards (`divided_map`) with it, so `targetedBy` and `Defined$ Targeted` see all of them. A triggered ability's triggering objects and trigger source live on the root; the stack walker hands them to each sub-ability that has none, as Forge reads them through `getRootAbility()`.
- **A copy of a spell gets its own host card, controlled by the copier.** `card_factory::copy_spell_ability_and_possibly_host` mirrors `CardFactory.copySpellAbilityAndPossiblyHost`: unless the copy effect has `UseOriginalHost$`, the host is a new card built from the original (`copied_permanent` set), put on the stack and remembered for `RememberNewCard$`. The copy and each of its sub-abilities get a new `SpellAbility::id`, as `SpellAbility.copy` gives them (`card_factory::copy_spell_ability`). Every ability the factory builds gets its own id as well (`build_spell_ability_for_host` replaces the cached template's), as each Java `SpellAbility` takes `nextId()`, so an id names one ability instance. A copy takes the fizzle check like any spell (`MagicStack.hasFizzled`); a permanent copy resolves down the normal permanent path and becomes a token, and a non-permanent host ceases to exist once it resolves or fizzles (`cease_to_exist_copied_spell`). While copies kept the original card, whatever an effect did to "the copy" (Choreographed Sparks' haste and sacrifice) landed on the original, and a copy never fizzled. Replicate (`cast_spell.rs`) still copies without a new host. A copy keeps the original's targets unless the copy effect has `MayChooseTarget$` (every Replicate copy counts as having it, as Forge's Replicate trigger does): then `SpellAbility::setup_new_targets` asks the copier's `PlayerAgent::choose_new_targets_for` once per targeting node, as `setupNewTargets` asks `chooseNewTargetsFor`, and restores the old targets when it answers `false`. `false` is the trait default and what Forge's AI and the harness's `DeterministicController` answer; the interactive agent opens a cancellable target prompt, and cancelling keeps the old targets.
- **ChangeTargets edits the target spell's stack entry in place.** `change_targets_effect.rs` mirrors `ChangeTargetsEffect.resolve`: it finds each target spell's entry (a `Defined$` spell through `MagicStack::get_instance_matching_spell_ability_id`, which matches `SpellAbility::id`), walks its sub-ability chain and either moves every targeting node to the `DefinedMagnet$` card when `SpellAbility::can_target` allows it, replaces the one target the chooser picks (`ChangeSingleTarget$`, `PlayerAgent::choose_target`), or asks `choose_new_targets_for` with `optional` false. Each object the spell did not target before gets `BecomesTarget`, and `BecomesTargetOnce` fires once. `RandomTarget$` is still a stub that draws one RNG value and changes nothing, and `TargetRestriction$` is not passed to the agent.
- **Clone's `AddSVars$`, `AddTriggers$`, `AddAbilities$` and `AddStaticAbilities$` read the host's SVars as they were before the copy.** Java's `CardFactory.getCloneStates` takes `origSVars` from the cloning ability's host first, so a card copying itself (Vesuvan Shapeshifter, Sakashima the Impostor) still finds the SVars the copy replaces. `clone_effect.rs` adds them before it sets the base counts and the changed-traits baseline, so they are the copy's own traits and the layer pass keeps them. `copy_permanent_effect::get_proto_type` is a second port of the same Java function and reads none of the four yet (Norin and Feldon, Delina, Wild Mage, Hofri Ghostforge).
- **Minimize SVar parsing — this is critical.** SVar resolution is late-bound by spec (`docs/forge-dsl-semantics.md` §5.2). Resolve SVars at the call site, lazily, and only for the SVar the engine actually needs. Do not eagerly walk the SVar graph at card construction or ability registration time. Do not pre-expand sub-ability chains into IR before they are reached. Do not re-parse the same SVar string on every reference — parse once, then reuse the parsed form. Aggressive eager parsing inflates load time, breaks the lazy semantics the spec relies on, and produces stale IR when card state changes (transform, copy, exile) alter the available SVars per invariant §10.7.

## Build & test

The Cargo workspace manifest is at the repository root, not `manabrew-rs/Cargo.toml`; run these commands from the root.

```bash
cargo build --workspace
cargo test --workspace
cargo check -pmanabrew-engine
yarn parity <test-name>          # see parity AGENTS.md
parity query --api <Api> --param <Key>   # which cards use it, and is it in the engine; see parity AGENTS.md
```

Any `manabrew-protocol` or `manabrew-relay-protocol` type change needs three regens: `yarn gen:types` (ts-rs → `src/protocol/`, gitignored), `yarn build:harness` (Java prompt DTOs, gitignored), and `yarn gen:protocol-docs` (`website/src/generated/protocol-types.json`, **committed** — CI fails the PR if stale).

`yarn gen:types` must remain idempotent because lint and build invoke it repeatedly in the same worktree. The protocol generator recreates `src/protocol/`, and xtask removes `src/api/hubTypes.ts` before ts-rs exports; do not change either path to append to existing generated files.

Run the engine unit tests as CI does, `cargo test --release -p manabrew-engine --lib`. In a debug build `match_waiting_triggers` and `resolve_stack` each take close to 1 MiB of stack, and `game_loop::tests::evoke_keeps_etb_triggers_when_spell_resolves` overflows the 2 MiB test thread (`RUST_MIN_STACK=4194304` also gets it through). It is not a recursion bug.

Known debt (as of 2026-07): `yarn lint:rust` (`cargo clippy --workspace -D warnings`) fails on a cold cache with ~110 pre-existing warnings in `manabrew-engine`, `forge-limited`, and `manabrew-server` (mostly `unused_variables`, `needless_bool`). Warm incremental caches hide them until a dependency edit forces a re-check, so a lint failure in crates your diff never touched is probably this — verify against `main` before assuming your change caused it. The parity regression suite (`regression.json`) likewise has pre-existing failing entries (e.g. `kaalia_regression`, `keyword_advanced`, `starter_*`); baseline on `main` before attributing failures to your change.

## Cardset archive

Every engine entrypoint (Tauri runtime, parity binary, self-hosted node, debugger) mmaps a single rkyv archive at startup: `src-tauri/resources/cardset.rkyv`. It bundles cards, tokens, editions, block data, and `lists/TypeLists.txt`. Path override: `CARDSET_ARCHIVE` env var. The web build has its own content-addressed copy at `public/wasm/cardset.<sha8>.rkyv` (resolved via `public/wasm/cardset.manifest.json`) produced by `yarn build:wasm`.

**Who builds it:**

| Trigger                                                                                                   | Output path                                  |
| --------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| `cargo build -p manabrew` (Tauri) — runs `src-tauri/build.rs`                                             | `src-tauri/resources/cardset.rkyv`           |
| `cargo run -p forge-cardset-archive --features build --release --bin build-cardset-archive` (manual / CI) | `src-tauri/resources/cardset.rkyv` (default) |
| `yarn build:wasm` — runs the bin then copies into `public/wasm/`                                          | `public/wasm/cardset.<sha8>.rkyv` + manifest |

`src-tauri/build.rs::needs_rebuild` checks both data-dir mtimes _and_ calls `forge_cardset_archive::load_checked()` on the existing file. A schema change in `forge-cardset-archive` (struct field added, reorder, type change) breaks `load_checked`, forcing a rebuild even when the source data is untouched. Without that guard, a stale archive on disk would survive a schema bump and panic at runtime with `pointer out of bounds`.

**CI gotcha:** workflows that build a single non-Tauri crate (`parity`, `self-hosted-node`, etc.) never trigger `src-tauri/build.rs`, so the archive isn't produced. Without an explicit build step they start up and panic with `Cardset archive not found at src-tauri/resources/cardset.rkyv`. All three parity workflows (`parity-self-hosted`, `parity-continuous`, `parity_ripper`) now have a `Build cardset archive` step before invoking the binary. Add the same step to any new workflow or local script that runs engine binaries directly.

**Web deploy gotcha (silent empty editions):** the production web archive is built _inside_ `Dockerfile.web` (not from the gitignored `public/wasm/` copy), which must `COPY` all five sources — `cardsfolder`, `tokenscripts`, **`editions`**, **`blockdata`**, **`lists/TypeLists.txt`** — that `scripts/build-wasm.mjs` passes to the builder. `build_archive_from_sources` hard-errors on a missing `cardsfolder` or `TypeLists.txt`; for `editions`/`blockdata` it silently emits an empty vec, so a build that forgets those dirs still succeeds but ships an archive with zero set data, and every limited draft fails with `unknown set: …` on web while Tauri (which always rebuilds from the full res tree) works. The production web image is built by CI (docker-images.yml) from the tag with submodules checked out, so a forge bump lands in the deployed archive with the next release.

**`TypeRegistry` is primed from the archive, at the point the archive loads.** `TypeRegistry::creature_types()` panics if `TypeRegistry::load` was never called, and the failure is delayed and card-specific — `is_creature_type` returns `false` when unloaded rather than panicking, so most of the engine degrades quietly and only changeling / `ChooseType` / tribal text-change cards blow up. Every entrypoint therefore calls `TypeRegistry::load(archive.type_lists.as_str(), archive.editions.iter().map(|e| e.raw.as_str()))` in the same function that loads the cardset bundle (the edition files add their own `[CreatureTypes]`, as `CardEdition.Reader` does in Java, and a `ChooseType` list that misses them draws a different index): `crates/wasm/src/card_loader.rs`, `self-hosted-node/.../rust_backend.rs`, `src-tauri/src/card_db.rs`, `parity/src/runner.rs`. Do **not** reintroduce an `include_str!` of `forge/forge-gui/res/lists/TypeLists.txt` — it makes the crate uncompilable without the submodule checked out, and the web build has no filesystem at runtime anyway. A new entrypoint gets this for free by loading the archive; priming it anywhere else risks running after the first card is used. The same load keeps every `[...Types]` section of `TypeLists.txt` (`TypeRegistry::is_subtype_in`, `is_land_type`): after a layer pass changes a card's types, `staticability/layer.rs` drops the subtypes that no longer fit its card types, as `CardType.sanisfySubtypes` does, and skips that step while the sections are not loaded (unit tests).

**DFC name lookups:** `CardDatabase::get_by_card_name` first tries the full string, then on miss splits at `" // "` and retries with just the front face. Split cards (`"Fire // Ice"`, stored as the full name) hit on the first attempt; Scryfall-style DFC names (`"Fable of the Mirror-Breaker // Reflection of Kiki-Jiki"`) resolve via the fallback. Deck imports that come straight from Scryfall don't need pre-processing for the engine.

**Flavor-name lookups:** a Universes Beyond printing is a `Variant:UniversesWithin:FlavorName:` line on the Universes Within card, not a card of its own, so `"A Most Helpful Weaver"` has to resolve to `Origin of Spider-Man`. Aliases come from two scans at archive load — `extract_flavor_aliases_from_edition_contents` for the edition files and `extract_flavor_aliases_from_card_scripts` for the card scripts. Both are needed because `register_flavor_aliases_for_card` only runs on the eager bulk-parse path, and every engine binary takes the lazy archive path, where nothing parses a card until it is asked for by name. A new alias source needs its own scan here; registering it at parse time is too late for the card that alias names.

## Web Image engine assets

The Forge Web Image build carries its whole asset tree **inside the wasm module** as a build-time resource: `scripts/build-forge-wasm.sh` builds `target/forge-cardset.rkyv` from the submodule's res dirs, frames it with `emit-forge-assets` into `target/forge-assets-framed.txt`, and `forge-harness/build-wasm.sh` embeds it via `-H:IncludeResources` (`FORGE_ASSETS` is mandatory — a module without the resource throws at boot rather than degrading). `WasmMain` streams it into the VFS with no JavaScript boundary crossing, so Forge's lazy card index comes up over every card (measured: ~470 ms unpack, ~500 ms full 33.7k-script index). Keep the bundle **uncompressed**: the Web Image build links no zip library, so a gzipped resource dies in `GZIPInputStream` with an `UnsatisfiedLinkError`.
